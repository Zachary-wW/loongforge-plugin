---
phase: 02-github-helpers-pr-issue-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/lib/idempotency.py
  - skills/adapt/lib/templates.py
  - skills/adapt/tests/lib/test_idempotency.py
  - skills/adapt/tests/lib/test_templates.py
autonomous: true
requirements: [RESUME-03, PR-03, ISSUE-01, ISSUE-02, ISSUE-03]

must_haves:
  truths:
    - "SHA256(run_id:phase:attempt:action_kind) is deterministic -- same inputs always produce same hex string"
    - "Idempotency footer is parseable -- extract key, run_id, phase, attempt, action from a body string"
    - "PR title contains run_id, phase, attempt, validator name"
    - "PR body for fix-PRs contains Fixes #N linkage"
    - "Issue body contains structured failure_signature, log excerpt, attempts.jsonl link, reproduction command"
    - "Dedup key (sha256 of phase:validator:kind:location) is embedded in issue body for cross-attempt dedup (D-02, ISSUE-03)"
  artifacts:
    - path: "skills/adapt/lib/idempotency.py"
      provides: "SHA256 key computation + footer format/parse + dedup key computation"
      exports: ["compute_idempotency_key", "format_footer", "parse_footer", "IdempotencyFooter", "compute_dedup_key"]
    - path: "skills/adapt/lib/templates.py"
      provides: "PR/issue/comment template rendering (with dedup key embedding in issue_body)"
      exports: ["pr_title", "pr_body", "issue_title", "issue_body", "dedup_comment", "agent_resume_comment", "closing_summary", "REQUIRED_LABELS"]
    - path: "skills/adapt/tests/lib/test_idempotency.py"
      provides: "Idempotency unit tests"
      min_lines: 50
    - path: "skills/adapt/tests/lib/test_templates.py"
      provides: "Template unit tests"
      min_lines: 55
  key_links:
    - from: "skills/adapt/lib/templates.py"
      to: "skills/adapt/lib/idempotency.py"
      via: "format_footer embedded in pr_body/issue_body; compute_dedup_key embedded in issue_body"
      pattern: "(format_footer|compute_dedup_key)\\("
---

<objective>
Create the idempotency key module and PR/issue/comment template module that all Phase 2 lifecycle methods depend on.

Purpose: Every PR and issue created by the loop MUST carry a deterministic idempotency footer (RESUME-03) and follow a templated format (PR-03, ISSUE-01, ISSUE-02). Issue bodies MUST also carry a dedup key for cross-attempt dedup (D-02, ISSUE-03). These two modules are the foundation that RealGhClient and FakeGhClient consume -- building them first means lifecycle implementations in Plan 02 can import and use them without forward references.

Output: `idempotency.py` (SHA256 key + footer format/parse + dedup key computation), `templates.py` (PR/issue/comment templates with dedup key embedding), plus their pytest suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-helpers-pr-issue-lifecycle/02-CONTEXT.md
@.planning/phases/02-github-helpers-pr-issue-lifecycle/02-RESEARCH.md
@.planning/phases/01-loop-foundation-contracts-schemas-safety-plumbing/01-SUMMARY.md

<interfaces>
<!-- Phase 1 artifacts consumed by this plan -->

From skills/adapt/lib/schema.py:
```python
class PrBlockOutput(BaseModel):
    model_config = ConfigDict(extra="ignore")
    number: Optional[int] = None
    url: Optional[str] = None
    head: Optional[str] = None
    base: Optional[str] = None
    state: Optional[Literal["open", "closed", "merged"]] = None
    merged_sha: Optional[str] = None
    idempotency_key: Optional[str] = None

class IssuesBlockOutput(BaseModel):
    model_config = ConfigDict(extra="ignore")
    opened: list[int] = Field(default_factory=list)
    closed: list[int] = Field(default_factory=list)
    escalated: list[int] = Field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotency module -- SHA256 key computation + footer format/parse + dedup key computation</name>
  <files>skills/adapt/lib/idempotency.py, skills/adapt/tests/lib/test_idempotency.py</files>
  <read_first>
    - skills/adapt/lib/idempotency.py (will be created)
    - .planning/phases/02-github-helpers-pr-issue-lifecycle/02-RESEARCH.md (Pattern 2: Idempotency Key as HTML Comment Footer)
    - .planning/REQUIREMENTS.md (RESUME-03 definition)
  </read_first>
  <behavior>
    - Test 1: compute_idempotency_key("run1", 1, 0, "base-pr") returns a 64-char lowercase hex string
    - Test 2: compute_idempotency_key is deterministic -- same inputs produce same output
    - Test 3: compute_idempotency_key produces different outputs for different inputs
    - Test 4: format_footer("run1", 1, 0, "base-pr") returns a string containing "<!-- adapt-skill:" and the key hex
    - Test 5: format_footer output includes run=run1, phase=1, attempt=0, action=base-pr
    - Test 6: parse_footer on a body containing a format_footer string returns IdempotencyFooter with correct fields
    - Test 7: parse_footer on a body with no footer returns None
    - Test 8: parse_footer on a body with multiple footers returns the first one
    - Test 9: Round-trip: compute_idempotency_key result matches key parsed from format_footer output
    - Test 10: compute_dedup_key(2, "loss-diff", {"kind": "numerical_mismatch", "location": "model.py:L42"}) returns a 64-char lowercase hex string
    - Test 11: compute_dedup_key is deterministic -- same (phase, validator, kind, location) always produces same key
    - Test 12: compute_dedup_key produces different keys for different (kind, location) pairs even with same phase and validator
    - Test 13: compute_dedup_key with empty failure_signature values (kind="", location="") still returns a valid 64-char hex
  </behavior>
  <action>
    Create `skills/adapt/lib/idempotency.py` with the following exports:

    1. `IdempotencyFooter` -- a `NamedTuple` with fields `run_id: str`, `phase: int`, `attempt: int`, `action: str`, `key: str`

    2. `compute_idempotency_key(run_id: str, phase: int, attempt: int, action_kind: str) -> str`
       - Implementation: `hashlib.sha256(f"{run_id}:{phase}:{attempt}:{action_kind}".encode()).hexdigest()`
       - Returns 64-char lowercase hex string
       - This is the RESUME-03 contract: same inputs always produce the same key, enabling crash-resume dedup

    3. `format_footer(run_id: str, phase: int, attempt: int, action_kind: str) -> str`
       - Implementation: compute key, then return the string:
         `"\n<!-- adapt-skill: run={run_id} phase={phase} attempt={attempt} action={action_kind} key={key} -->\n"`
       - The footer is an HTML comment so it is invisible in rendered Markdown but parseable by machines
       - ALSO include a visible machine-readable fallback line immediately before the HTML comment:
         `[adapt-skill-key: {key}]`
       - This fallback addresses the open question from RESEARCH about whether GitHub search indexes HTML comments.
         The visible line ensures searchability even if HTML comments are not indexed.

    4. `parse_footer(body: str) -> Optional[IdempotencyFooter]`
       - Search body for the HTML comment pattern: `<!-- adapt-skill: run=(\S+) phase=(\d+) attempt=(\d+) action=(\S+) key=([a-f0-9]{64}) -->`
       - If found, return IdempotencyFooter with extracted fields
       - If not found, return None
       - Use `re.search` with the pattern; the regex must capture all 5 fields

    5. `compute_dedup_key(phase: int, validator_name: str, failure_signature: dict) -> str`
       - Implementation: `hashlib.sha256(f"{phase}:{validator_name}:{failure_signature.get('kind', '')}:{failure_signature.get('location', '')}".encode()).hexdigest()`
       - Returns 64-char lowercase hex string
       - This is the ISSUE-03 / D-02 dedup contract: same (phase, validator, kind, location) always produces the same key,
         enabling issue dedup across attempts. The key is DIFFERENT from the idempotency key: idempotency key
         is unique per (run_id, phase, attempt, action_kind) for crash-resume; dedup key is unique per
         (phase, validator, kind, location) for issue dedup. They serve different purposes and must never
         be conflated -- the checker caught a blocker where these were mixed in Plan 02's open_issue.

    Create `skills/adapt/tests/lib/test_idempotency.py` with pytest tests covering all 13 behaviors listed above.
    Use `from skills.adapt.lib.idempotency import compute_idempotency_key, format_footer, parse_footer, IdempotencyFooter, compute_dedup_key`.
  </action>
  <verify>
    <automated>python3 -m pytest skills/adapt/tests/lib/test_idempotency.py -x -q</automated>
  </verify>
  <done>
    - compute_idempotency_key produces deterministic 64-char hex SHA256
    - format_footer generates HTML comment with adapt-skill metadata + visible [adapt-skill-key: ...] fallback
    - parse_footer extracts IdempotencyFooter from body or returns None
    - Round-trip: key from compute_idempotency_key matches key parsed from format_footer output
    - compute_dedup_key produces deterministic 64-char hex SHA256 from (phase, validator, kind, location)
    - compute_dedup_key and compute_idempotency_key produce different outputs for the same phase (different input tuples)
    - All 13+ idempotency tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template module -- PR/issue/comment templates with label constants and dedup key embedding</name>
  <files>skills/adapt/lib/templates.py, skills/adapt/tests/lib/test_templates.py</files>
  <read_first>
    - skills/adapt/lib/templates.py (will be created)
    - skills/adapt/lib/idempotency.py (created in Task 1 -- import format_footer, compute_dedup_key)
    - .planning/phases/02-github-helpers-pr-issue-lifecycle/02-CONTEXT.md (D-04: template constraints)
    - .planning/REQUIREMENTS.md (PR-03, ISSUE-01, ISSUE-02, ISSUE-03)
    - .planning/phases/02-github-helpers-pr-issue-lifecycle/02-RESEARCH.md (Code Examples section)
  </read_first>
  <behavior>
    - Test 1: pr_title("run-abc", 2, 1, "phase2-conversion", "base") returns a string containing "run-abc", "phase-2", "attempt-1", "phase2-conversion"
    - Test 2: pr_title with kind="fix" includes "fix" in the title
    - Test 3: pr_body with fixes_issue=5 contains "Fixes #5" (exact GitHub-recognized syntax)
    - Test 4: pr_body without fixes_issue does NOT contain "Fixes"
    - Test 5: pr_body calls format_footer and includes the idempotency footer
    - Test 6: issue_title(2, "loss-diff", "numerical_mismatch") returns a string containing "phase-2", "loss-diff", "numerical_mismatch"
    - Test 7: issue_body contains structured "## Failure Signature" section with kind/location/expected/actual fields
    - Test 8: issue_body contains "## Log Excerpt" section, "## Attempts Log" section with link, and "## Reproduction" section with command
    - Test 9: issue_body with non-empty failure_signature contains "[dedup-key: " followed by a 64-char hex string (D-02, ISSUE-03)
    - Test 10: issue_body with empty failure_signature dict does NOT contain "[dedup-key:"
    - Test 11: dedup_comment(3, "last 10 lines...", "2026-06-22T10:00:00Z") returns a string containing "attempt-3", the log excerpt, and timestamp
    - Test 12: agent_resume_comment("run-abc", 2) returns a string containing "/agent-resume"
    - Test 13: closing_summary("run-abc", 2, "validator_passed") returns a string containing the run_id, phase, and outcome
    - Test 14: REQUIRED_LABELS is a list containing "loongforge-adapt"
  </behavior>
  <action>
    Create `skills/adapt/lib/templates.py` with the following exports:

    1. `REQUIRED_LABELS: list[str] = ["loongforge-adapt"]`
       - Base label constant. Callers add `f"run-{run_id}"` and `f"phase-{phase}"` at runtime.
       - This addresses ISSUE-04: all bot PRs/issues carry these labels.

    2. `def pr_title(run_id: str, phase: int, attempt: int, validator: str = "", kind: str = "base") -> str`
       - Template: `[adapt][{kind}][run-{run_id}] phase-{phase} attempt-{attempt}{validator_suffix}`
       - Where `validator_suffix = f" ({validator})" if validator else ""`
       - `kind` is one of "base", "fix", "diagnose" -- appears in brackets
       - Example output: `[adapt][base][run-abc123] phase-2 attempt-1 (phase2-conversion)`
       - This addresses PR-03: title contains run_id, phase, attempt, validator name

    3. `def pr_body(run_id: str, phase: int, attempt: int, kind: str = "base", validator: str = "", diff_summary: str = "", fixes_issue: Optional[int] = None) -> str`
       - Build the body from these sections:
         a. `## Summary` -- one-liner: `Adapt skill {kind} PR for phase {phase}, attempt {attempt}.`
         b. `## Validator` -- `{validator}` if non-empty, else "N/A (base PR)"
         c. `## Diff Summary` -- `{diff_summary}` if non-empty, else "(see PR diff)"
         d. `## Linked Issue` -- `Fixes #{fixes_issue}` if fixes_issue is not None, else "N/A"
       - Append idempotency footer via `format_footer(run_id, phase, attempt, f"{kind}-pr")`
       - The `Fixes #N` syntax is exact per GitHub recognition rules (ISSUE-02)

    4. `def issue_title(phase: int, validator_name: str, failure_kind: str) -> str`
       - Template: `[adapt] phase-{phase} {validator_name}: {failure_kind}`
       - Example: `[adapt] phase-2 loss-diff: numerical_mismatch`

    5. `def issue_body(phase: int, validator_name: str, failure_signature: dict, log_excerpt: str, attempts_jsonl_link: str, reproduction_cmd: str, run_id: str = "", attempt: int = 0) -> str`
       - Build the body from these sections:
         a. `## Failure Signature` -- structured table:
            ```
            | Field     | Value |
            |-----------|-------|
            | kind      | {failure_signature.get("kind", "")} |
            | location  | {failure_signature.get("location", "")} |
            | expected  | {failure_signature.get("expected", "")} |
            | actual    | {failure_signature.get("actual", "")} |
            ```
         b. `## Log Excerpt` -- `{log_excerpt}` (caller passes last N lines)
         c. `<details><summary>Full Log</summary>` -- placeholder; caller fills from file
         d. `## Attempts Log` -- `[attempts.jsonl]({attempts_jsonl_link})`
         e. `## Reproduction` -- `{reproduction_cmd}` in a code block
       - **Dedup key embedding (D-02, ISSUE-03):** If `failure_signature` is non-empty and contains
         at least one of "kind" or "location", compute `dedup_key = compute_dedup_key(phase, validator_name, failure_signature)`
         and add a visible line `[dedup-key: {dedup_key}]` immediately before the idempotency footer.
         This line is searchable via `gh issue list --search "dedup-key: {hex}"` and enables cross-attempt
         dedup -- the same (phase, validator, kind, location) always maps to the same dedup key hash,
         allowing `find_by_dedup_key` to locate existing issues regardless of which attempt created them.
         If `failure_signature` is empty or `{}`, do NOT embed a dedup key line (no meaningful dedup possible).
       - Append idempotency footer via `format_footer(run_id, phase, attempt, "issue")`
       - This addresses ISSUE-01: structured failure_signature, log excerpt, attempts.jsonl link, reproduction command

    6. `def dedup_comment(attempt: int, log_excerpt: str, timestamp: str) -> str`
       - Template:
         ```
         **Attempt {attempt}** ({timestamp})

         <details><summary>Log excerpt</summary>

         {log_excerpt}

         </details>
         ```
       - This addresses ISSUE-03 / D-02: appended to existing issue instead of creating duplicate

    7. `def agent_resume_comment(run_id: str, phase: int) -> str`
       - Template:
         ```
         :warning: Human commits detected on this branch.

         The adapt skill loop is paused. To resume, please:
         1. Review the commits above
         2. Run `/agent-resume` or invoke `loongforge-adapt --resume <run_dir> --from-phase {phase}`

         Run ID: `{run_id}` | Phase: {phase}
         ```
       - This addresses PR-05 / D-01: posts this comment when human commits are detected

    8. `def closing_summary(run_id: str, phase: int, outcome: str) -> str`
       - Template:
         ```
         Run `{run_id}` phase-{phase} completed: **{outcome}**

         Closing as part of run finalization.
         ```
       - Used at run end to close auxiliary issues (ISSUE-04)

    Import `compute_dedup_key` from `skills.adapt.lib.idempotency` (in addition to `format_footer`).

    Create `skills/adapt/tests/lib/test_templates.py` with pytest tests covering all 14+ behaviors listed above.
    Import from `skills.adapt.lib.templates` and `skills.adapt.lib.idempotency` (for footer and dedup key presence checks).
  </action>
  <verify>
    <automated>python3 -m pytest skills/adapt/tests/lib/test_templates.py skills/adapt/tests/lib/test_idempotency.py -x -q</automated>
  </verify>
  <done>
    - pr_title contains run_id, phase, attempt, validator, kind
    - pr_body for fix-PRs contains "Fixes #N" in exact GitHub-recognized syntax
    - pr_body includes idempotency footer via format_footer
    - issue_body contains structured failure_signature table, log excerpt, attempts.jsonl link, reproduction command
    - issue_body with non-empty failure_signature contains "[dedup-key: {hex}]" for cross-attempt dedup (D-02, ISSUE-03)
    - issue_body with empty failure_signature does NOT contain dedup key
    - dedup_comment contains attempt number, log excerpt, timestamp
    - agent_resume_comment contains "/agent-resume" and run_id and phase
    - closing_summary contains run_id, phase, outcome
    - REQUIRED_LABELS contains "loongforge-adapt"
    - All template + idempotency tests pass
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest skills/adapt/tests/lib/test_idempotency.py skills/adapt/tests/lib/test_templates.py -x -q` exits 0
2. `python3 -c "from skills.adapt.lib.idempotency import compute_idempotency_key, format_footer, parse_footer, IdempotencyFooter, compute_dedup_key"` exits 0
3. `python3 -c "from skills.adapt.lib.templates import pr_title, pr_body, issue_title, issue_body, dedup_comment, agent_resume_comment, closing_summary, REQUIRED_LABELS"` exits 0
4. Existing test suite still passes: `python3 -m pytest skills/adapt/tests/lib/ -x -q` exits 0
</verification>

<success_criteria>
- idempotency.py: compute_idempotency_key is deterministic (SHA256), format_footer generates HTML comment + visible fallback, parse_footer extracts IdempotencyFooter or returns None, compute_dedup_key produces deterministic dedup-specific SHA256
- templates.py: pr_title/body follow PR-03 format, issue_body follows ISSUE-01 format with [dedup-key: ...] embedding for ISSUE-03, fix-PRs carry "Fixes #N", REQUIRED_LABELS constant defined
- Dedup key and idempotency key are distinct: different inputs, different hashes, different purposes (cross-attempt dedup vs crash-resume)
- All new + existing tests pass (103 existing + ~24 new)
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-helpers-pr-issue-lifecycle/02-01-SUMMARY.md`
</output>
