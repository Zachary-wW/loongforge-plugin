---
phase: 02-github-helpers-pr-issue-lifecycle
verified: 2026-06-22T12:00:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 2: GitHub Helpers -- PR & Issue Lifecycle Verification Report

**Phase Goal:** A maker agent can branch, open, label, and merge PRs and issues against both external repos through a single typed adapter, with idempotency keys and policy guards (validator-path write-protection, force-push refusal) that survive crashes.
**Verified:** 2026-06-22
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

Derived from ROADMAP.md Success Criteria:

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `gh_client.py` exposes `create_branch`, `open_pr`, `merge_pr`, `open_issue`, `close_issue`, and `find_by_idempotency_key`; each call records a SHA256(run_id+phase+attempt+action_kind) footer and re-invocation with the same key returns the existing artifact rather than creating a duplicate (RESUME-03). | VERIFIED | All 6 lifecycle methods + 2 find methods implemented on both RealGhClient and FakeGhClient. `compute_idempotency_key` produces deterministic SHA256. `format_footer` embeds it. `find_by_idempotency_key` locates by key. Test `test_idempotency_across_resets` confirms crash-resume behavior. |
| 2 | PRs are created on branch `adapt/<run_id>/phase<N>/attempt<K>` (PR-04) with title/body/labels matching the templated format containing run_id, phase, attempt, validator name, and idempotency footer (PR-03); every fix-PR carries `Fixes #N` linkage (ISSUE-02); direct push to default branch is refused (PR-01); merge uses `gh pr merge --squash` (PR-02). | VERIFIED | `_BRANCH_RE` regex enforces branch naming. `pr_title`/`pr_body` templates used in both Real and Fake clients. `Fixes #N` generated by `pr_body` when `fixes_issue` is set. `DirectPushError` raised when base matches default branch. `merge_pr` defaults to `--squash`. Tests: `test_create_branch_direct_push`, `test_open_pr_fixes_issue`, `test_merge_pr_ok`. |
| 3 | Any PR diff touching protected paths is auto-rejected and converted to `human_needed` escalation (PR-06); force-push to a branch containing non-bot commits is refused, with `/agent-resume` comment posted (PR-05). | VERIFIED | `ProtectedPathError` raised in `open_pr` when `is_protected()` matches. `HumanCommitError` raised when `git log --format=%ae` detects non-bot emails. `/agent-resume` comment posted on existing PR before raising error. Tests: `test_open_pr_protected_path`, `test_open_pr_human_commit_with_existing_pr`, `test_open_pr_human_commit_no_existing_pr`. |
| 4 | Issue creation contains structured `failure_signature`, log excerpt, attempts.jsonl link, and reproduction command (ISSUE-01); same (phase, validator_name, failure_signature) reuses the open issue and appends a comment (ISSUE-03); every fix-PR closes its issue on merge (ISSUE-02). | VERIFIED | `issue_body` template generates structured table with all 4 fields. `open_issue` computes `dedup_key` via `compute_dedup_key` and calls `find_by_dedup_key` -- if found, appends `dedup_comment` instead of creating new issue. `Fixes #N` in fix-PR body causes GitHub auto-close on merge. Tests: `test_open_issue_dedup`, `test_open_issue_ok`, `test_full_fix_loop`. |
| 5 | All bot-created PRs/issues carry labels `loongforge-adapt`, `run-<id>`, `phase-<N>`; opening, commenting on, closing, and re-finding artifacts each work end-to-end against `FakeGhClient` in pytest (ISSUE-04). | VERIFIED | `REQUIRED_LABELS` + `run-{run_id}` + `phase-{phase}` applied in `open_pr` and `open_issue` (both Real and Fake). Tests: `test_open_pr_labels`, `test_open_issue_labels`, `test_full_fix_loop` (verifies labels on all artifacts). 67 Phase 2 tests pass. 170 total tests green. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `skills/adapt/lib/idempotency.py` | SHA256 key computation + footer format/parse + dedup key computation | VERIFIED | 84 lines. Exports: `compute_idempotency_key`, `format_footer`, `parse_footer`, `IdempotencyFooter`, `compute_dedup_key`. Deterministic, round-trip verified. |
| `skills/adapt/lib/templates.py` | PR/issue/comment template rendering with dedup key embedding | VERIFIED | 187 lines. Exports: `pr_title`, `pr_body`, `issue_title`, `issue_body`, `dedup_comment`, `agent_resume_comment`, `closing_summary`, `REQUIRED_LABELS`. All template functions are pure. |
| `skills/adapt/tests/lib/test_idempotency.py` | Idempotency unit tests | VERIFIED | 201 lines (min_lines: 50). 19 tests covering all 13 planned behaviors plus extras. |
| `skills/adapt/tests/lib/test_templates.py` | Template unit tests | VERIFIED | 193 lines (min_lines: 55). 17 tests covering all 14 planned behaviors plus extras. |
| `skills/adapt/lib/gh_client.py` | RealGhClient lifecycle methods + FakeGhClient state machine + policy exceptions + find_by_dedup_key | VERIFIED | 652 lines. All 11 Protocol methods implemented on both Real and Fake. No NotImplementedError stubs. Contains `create_branch`, `open_pr`, `merge_pr`, `open_issue`, `close_issue`, `find_by_idempotency_key`, `find_by_dedup_key`. |
| `skills/adapt/tests/lib/test_gh_client_lifecycle.py` | Lifecycle integration tests via FakeGhClient | VERIFIED | 531 lines (min_lines: 120). 31 tests covering PR lifecycle, issue lifecycle, policy guards, dedup, end-to-end fix loop. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `RealGhClient.open_pr` | `redact.py` | `redact()` called on body before gh pr create | WIRED | Line 24: `from skills.adapt.lib.redact import redact`. Lines 246, 260: `rr = redact(comment)`, `rr = redact(body)`. |
| `RealGhClient.open_pr` | `protected_paths.py` | `is_protected()` checked on diff before gh pr create | WIRED | Line 25: `from skills.adapt.lib.protected_paths import is_protected`. Line 217: `protected = [f for f in changed if is_protected(f)]`. |
| `RealGhClient.open_pr` | `templates.py` | `pr_title`, `pr_body`, `agent_resume_comment` imported and used | WIRED | Lines 27-30: imports. Lines 245, 257-258: `agent_resume_comment(run_id, phase)`, `pr_title(...)`, `pr_body(...)`. |
| `RealGhClient.open_issue` | `idempotency.py` | `compute_dedup_key` + `find_by_dedup_key` called for dedup search | WIRED | Line 26: `from skills.adapt.lib.idempotency import ... compute_dedup_key`. Lines 306-307: `dedup_key = compute_dedup_key(...)`, `existing = self.find_by_dedup_key(...)`. |
| `FakeGhClient.open_pr` | `FakeGhClient._pr_store` | PRs stored by number in dict for find_by_idempotency_key and merge_pr | WIRED | Line 541: `self._pr_store[(owner_repo, number)] = record`. Lines 548-549, 637-639: `merge_pr` and `find_by_idempotency_key` search `_pr_store`. |
| `FakeGhClient.open_issue` | `FakeGhClient._issue_store` | Issues stored by number with dedup_key for find_by_dedup_key dedup | WIRED | Line 612: `self._issue_store[(owner_repo, number)] = record`. Lines 649-651: `find_by_dedup_key` searches `_issue_store` by `dedup_key` and `state == "open"`. |
| `templates.py` | `idempotency.py` | `format_footer` embedded in pr_body/issue_body; `compute_dedup_key` embedded in issue_body | WIRED | Line 17: `from skills.adapt.lib.idempotency import compute_dedup_key, format_footer`. Line 73: `body += format_footer(...)`. Line 142-143: `dedup_key = compute_dedup_key(...)`, `body += f"...[dedup-key: {dedup_key}]"`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `FakeGhClient.open_issue` dedup path | `dedup_key` | `compute_dedup_key(phase, validator_name, failure_signature)` | Yes -- SHA256 hash computed from real inputs | FLOWING |
| `FakeGhClient.open_pr` label list | `all_labels` | `REQUIRED_LABELS + [f"run-{run_id}", f"phase-{phase}"]` | Yes -- labels computed from run parameters | FLOWING |
| `RealGhClient.open_pr` body | `body` | `pr_body(run_id, phase, attempt, kind, validator, diff_summary, fixes_issue)` then `redact(body)` | Yes -- template-rendered and redacted | FLOWING |
| `RealGhClient.open_issue` dedup check | `existing` | `self.find_by_dedup_key(owner_repo, dedup_key)` | Yes -- searches GitHub for matching issues | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All Phase 2 tests pass | `python3 -m pytest skills/adapt/tests/lib/test_idempotency.py skills/adapt/tests/lib/test_templates.py skills/adapt/tests/lib/test_gh_client_lifecycle.py -x -q` | 67 passed in 0.16s | PASS |
| Full test suite passes (Phase 1 + Phase 2) | `python3 -m pytest skills/adapt/tests/lib/ -x -q` | 170 passed in 46.12s | PASS |
| Idempotency module exports correct | `python3 -c "from skills.adapt.lib.idempotency import compute_idempotency_key, format_footer, parse_footer, IdempotencyFooter, compute_dedup_key"` | exit 0 | PASS |
| Template module exports correct | `python3 -c "from skills.adapt.lib.templates import pr_title, pr_body, issue_title, issue_body, dedup_comment, agent_resume_comment, closing_summary, REQUIRED_LABELS"` | exit 0 | PASS |
| Policy exceptions importable | `python3 -c "from skills.adapt.lib.gh_client import ProtectedPathError, HumanCommitError, DirectPushError"` | exit 0 | PASS |
| FakeGhClient lifecycle works end-to-end | Full dedup flow test (open issue, dedup, close, find) | All assertions pass | PASS |
| No NotImplementedError stubs | `grep -c NotImplementedError gh_client.py` | 0 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PR-01 | 02-02 | Direct push to default branch forbidden | SATISFIED | `DirectPushError` raised in `create_branch` (Real + Fake). Test: `test_create_branch_direct_push`. |
| PR-02 | 02-02 | Base PR must merge before validator runs; merge uses --squash | SATISFIED | `merge_pr` defaults `method="squash"` and uses `--squash`/`--delete-branch`. Test: `test_merge_pr_ok`. |
| PR-03 | 02-01 | PR title/body/labels follow templated format with run_id, phase, attempt, validator name, idempotency footer | SATISFIED | `pr_title`/`pr_body` templates used in both clients. Footer embedded via `format_footer`. Tests: `test_pr_title_contains_metadata`, `test_open_pr_ok`, `test_open_pr_idempotency_footer`. |
| PR-04 | 02-02 | Branch naming: `adapt/<run_id>/phase<N>/attempt<K>` | SATISFIED | `_BRANCH_RE` regex validates branch names in both clients. Test: `test_create_branch_invalid_name`. |
| PR-05 | 02-02 | Force-push to branch with non-bot commits refused; posts /agent-resume comment | SATISFIED | `HumanCommitError` raised when `git log --format=%ae` detects non-bot emails. `agent_resume_comment` posted on existing PR. Tests: `test_open_pr_human_commit_no_existing_pr`, `test_open_pr_human_commit_with_existing_pr`. |
| PR-06 | 02-02 | Validator-path edits auto-rejected | SATISFIED | `ProtectedPathError` raised when `is_protected()` matches diff files. Test: `test_open_pr_protected_path`. |
| ISSUE-01 | 02-01 | Issue body contains structured failure_signature, log excerpt, attempts.jsonl link, reproduction command | SATISFIED | `issue_body` template generates all required sections. Test: `test_issue_body_contains_failure_signature`, `test_issue_body_contains_required_sections`. |
| ISSUE-02 | 02-01 | Every fix-PR carries `Fixes #N` linkage | SATISFIED | `pr_body` generates `Fixes #{fixes_issue}` when `fixes_issue` is set. Test: `test_pr_body_fixes_issue`. |
| ISSUE-03 | 02-01 | Same (phase, validator, failure_signature) reuses open issue | SATISFIED | `open_issue` calls `compute_dedup_key` then `find_by_dedup_key`; if found, appends `dedup_comment`. Test: `test_open_issue_dedup`. |
| ISSUE-04 | 02-01 | All bot PRs/issues carry labels loongforge-adapt, run-<id>, phase-<N> | SATISFIED | Labels computed as `REQUIRED_LABELS + [f"run-{run_id}", f"phase-{phase}"]` in both `open_pr` and `open_issue`. Tests: `test_open_pr_labels`, `test_open_issue_labels`. |
| RESUME-03 | 02-01 | Idempotency keys prevent duplicate PR/issue creation across crash-resume | SATISFIED | `compute_idempotency_key` is deterministic SHA256. `find_by_idempotency_key` locates by key. Test: `test_idempotency_across_resets`, `test_find_pr_by_idempotency_key`. |

No orphaned requirements found. All 11 requirement IDs from PLAN frontmatter are accounted for. REQUIREMENTS.md maps no additional IDs to Phase 2 beyond these 11.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `gh_client.py` | 169, 173, 178, 182 | `return {}` in preflight error paths | Info | Phase 1 code. `return {}` is the correct behavior for repo_permissions/branch_protection when `gh api` fails (empty dict = no permissions / no protection). Not a stub. |

No blocker or warning anti-patterns found. No TODO/FIXME/PLACEHOLDER. No NotImplementedError stubs. No empty handlers.

### Human Verification Required

None -- all success criteria are mechanically verifiable through code inspection and test execution.

### Gaps Summary

No gaps found. All 5 observable truths verified, all 6 artifacts exist with substantive content and correct wiring, all 7 key links are wired, all 11 requirement IDs are satisfied, and 170 tests pass. The phase goal -- a single typed adapter with idempotency keys and policy guards that survive crashes -- is achieved.

---

_Verified: 2026-06-22_
_Verifier: Claude (gsd-verifier)_
