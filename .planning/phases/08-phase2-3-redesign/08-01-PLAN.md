---
phase: 08-phase2-3-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/references/phases/phase2/agent.md
  - skills/adapt/references/phases/phase2/phase2_output_schema.yaml
autonomous: true
requirements: [P2R-01, P2R-02, P2R-03]

must_haves:
  truths:
    - "Phase 2 agent.md uses bridge_mapping_path as primary input for weight_map; model_spec_path is legacy fallback only"
    - "Phase 2 Input Contract reads generated_loongforge_files and generated_megatron_files from Phase 1 output"
    - "Phase 2 Step 0 reads bridge_mapping.conversion_requirements instead of model_spec.conversion_requirements and reference_contract_path"
    - "Phase 2 output schema adds bridge_mapping_consumed and source.bridge_mapping_path"
  artifacts:
    - path: "skills/adapt/references/phases/phase2/agent.md"
      provides: "Phase 2 agent manual with bridge-mapping-first, dual-repo consumption"
      min_lines: 400
    - path: "skills/adapt/references/phases/phase2/phase2_output_schema.yaml"
      provides: "Extended Phase 2 output schema with bridge_mapping_consumed and dual-repo fields"
      contains: "bridge_mapping_consumed"
  key_links:
    - from: "skills/adapt/references/phases/phase2/agent.md"
      to: "bridge_mapping.yaml"
      via: "Step 1 reads bridge_mapping.component_bridge[].weight_map as authoritative name map"
      pattern: "bridge_mapping_path"
    - from: "skills/adapt/references/phases/phase2/agent.md"
      to: "phase1_output.yml"
      via: "Input Contract reads generated_loongforge_files and generated_megatron_files"
      pattern: "generated_loongforge_files|generated_megatron_files"
    - from: "skills/adapt/references/phases/phase2/agent.md"
      to: "phase2_output_schema.yaml"
      via: "Output Contract references schema with bridge_mapping_consumed field"
      pattern: "bridge_mapping_consumed"
---

<objective>
Rewrite Phase 2 agent.md to consume bridge_mapping as primary input for weight name mapping, support dual-repo file consumption from Phase 1 output, and replace reference_contract_path/model_spec.conversion_requirements with bridge_mapping fields. Update phase2_output_schema.yaml with bridge_mapping_consumed and dual-repo fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-phase2-3-redesign/08-CONTEXT.md

@skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml
@skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml
@skills/adapt/references/phases/phase2/agent.md
@skills/adapt/references/phases/phase2/phase2_output_schema.yaml
@skills/adapt/references/phases/phase1/phase1_output_schema.yaml
@skills/adapt/lib/schema.py
</context>

<tasks>

<task type="auto">
  <name>Rewrite Phase 2 agent.md with bridge-mapping-first input and dual-repo consumption</name>
  <files>skills/adapt/references/phases/phase2/agent.md</files>
  <read_first>
  - skills/adapt/references/phases/phase2/agent.md — current Phase 2 agent manual (512 lines)
  - .planning/phases/08-phase2-3-redesign/08-CONTEXT.md — decisions D-01 through D-03
  - skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml — primary input schema (component_bridge, weight_map, conversion_requirements, gaps)
  - skills/adapt/references/phases/phase1/phase1_output_schema.yaml — Phase 1 output with generated_loongforge_files and generated_megatron_files
  - .planning/phases/07-phase1-redesign/07-CONTEXT.md — D-09 pattern (bridge_mapping_path as primary)
  </read_first>
  <action>
REWRITE the agent.md file. The rewrite preserves the general structure (Role, State Machine, Input Contract, Loop Engineering Hooks, Prerequisites, Steps 0-5, Output Contract, Error Handling) but makes these specific changes per decisions D-01 through D-03:

**1. Input Contract section -- bridge_mapping_path as primary (per D-01):**

Change the "Key path usage rule" to: bridge_mapping_path is the PRIMARY input for Phase 2 weight name mapping. model_spec_path is a legacy fallback used ONLY when bridge_mapping_path is absent.

From `phase0_output.yml`, the primary fields are now:
- `artifacts.bridge_mapping_path` — PRIMARY input for weight_map, conversion_requirements, gaps
- `artifacts.model_spec_path` — legacy fallback (used when bridge_mapping_path absent)
- `artifacts.hf_analysis_path` — for weight_structure and components (when available)
- `artifacts.reference_contract_path` — DEPRECATED; its fields are now in bridge_mapping.yaml

From `phase1_output.yml`, replace the current `generated_files` reference with split lists (per D-02):
- `artifacts.generated_loongforge_files` — PRIMARY list of LoongForge files generated by Phase 1
- `artifacts.generated_megatron_files` — PRIMARY list of Megatron files generated by Phase 1
- `artifacts.generated_files` — LEGACY fallback (when split fields absent, treat as LoongForge-only)

Add an explicit note: "When generated_loongforge_files and generated_megatron_files are absent (legacy Phase 1 output), fall back to reading generated_files as LoongForge-only files."

**2. Step 0 -- Conversion Contract Preflight (per D-03):**

Replace current text "Read `model_spec.conversion_requirements` and optional `phase0_output.artifacts.reference_contract_path`" with:
"Read `bridge_mapping.conversion_requirements` as the primary source. When bridge_mapping is absent, fall back to `model_spec.conversion_requirements`. The `reference_contract_path` field is deprecated; its conversion_requirements content has been absorbed into bridge_mapping.yaml (per Phase 6 D-05)."

The conversion_contract_preflight record gains a new field: `bridge_mapping_consumed: true|false`.

**3. Step 1 -- Weight structure analysis (per D-01):**

The current Step 1 text already mentions bridge_mapping.weight_map as "pre-mapped weight name correspondences" but positions it as supplementary. RESTRUCTURE to make it authoritative:

Replace the current Step 1 bridge_mapping paragraph with:
"When bridge_mapping.yaml exists, read `bridge_mapping.component_bridge[].weight_map` as the AUTHORITATIVE weight name mapping. Each weight_map entry maps an HF parameter name to a Megatron parameter name with shape hints. These entries are the starting point for name_map generation — they MUST be used as the primary source, not just a hint. Cross-verify against HF checkpoint index and LoongForge/Megatron source code (Step 1 Source Discovery Mandates still apply). When weight_map entries conflict with source discovery, record the discrepancy in the step trace and prefer the source-discovered mapping (source is ground truth), but flag it as a bridge_mapping inconsistency for Phase 0 review."

Add for dual-repo consumption (per D-02): "For Mcore module path discovery, read files from both generated_loongforge_files and generated_megatron_files. Megatron-generated files may introduce new modules with custom weight loading that affects the convert YAML name_map."

**4. Prerequisites section:**

Replace "Read `phase0_output.artifacts.model_spec_path` and confirm:" with:
"Read `phase0_output.artifacts.bridge_mapping_path` as primary (when present), or `phase0_output.artifacts.model_spec_path` as legacy fallback. Confirm:"

**5. Output Contract section (per D-06):**

Add to the JSON result `details` object: `bridge_mapping_consumed: true|false` (true when bridge_mapping was used as primary input).

Add to the `details.generated_files` output: note that generated_files may include both LoongForge and Megatron paths.

The output schema path reference remains the same, but the schema itself is updated in Task 2.

**6. Keep unchanged:** State Machine, Loop Engineering Hooks, Steps 2-5, Execution Rules, Execution Progress Table, Error Handling table. The core conversion logic (Step 2-5) is unchanged; only input consumption changes.
  </action>
  <verify>
    grep -c "bridge_mapping_path" skills/adapt/references/phases/phase2/agent.md | xargs -I{} test {} -ge 5 && \
    grep -q "model_spec_path.*legacy.*fallback\|legacy fallback.*model_spec\|legacy.*fallback.*model_spec_path" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "generated_loongforge_files" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "generated_megatron_files" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "bridge_mapping.conversion_requirements" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "reference_contract_path.*deprecated\|DEPRECATED.*reference_contract" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "AUTHORITATIVE\|authoritative" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase2/agent.md && \
    grep -q "generated_files.*LEGACY\|legacy.*generated_files\|legacy fallback.*generated_files" skills/adapt/references/phases/phase2/agent.md
  </verify>
  <done>
Phase 2 agent.md rewritten with bridge_mapping_path as primary input (model_spec_path as legacy fallback), Input Contract reads generated_loongforge_files + generated_megatron_files from Phase 1 output (with generated_files as legacy fallback), Step 0 reads bridge_mapping.conversion_requirements instead of model_spec.conversion_requirements + reference_contract_path, Step 1 uses bridge_mapping.component_bridge[].weight_map as authoritative name map source, output contract includes bridge_mapping_consumed field.
  </done>
</task>

<task type="auto">
  <name>Update phase2_output_schema.yaml with bridge_mapping_consumed and dual-repo fields</name>
  <files>skills/adapt/references/phases/phase2/phase2_output_schema.yaml</files>
  <read_first>
  - skills/adapt/references/phases/phase2/phase2_output_schema.yaml — current schema (95 lines)
  - .planning/phases/08-phase2-3-redesign/08-CONTEXT.md — decisions D-06
  - skills/adapt/references/phases/phase1/phase1_output_schema.yaml — Phase 1 pattern for bridge_mapping_consumed and generated_megatron_files
  </read_first>
  <action>
Update phase2_output_schema.yaml with these specific additions per D-06:

**1. In the `source:` section**, add after `model_spec_path`:
```yaml
  bridge_mapping_path: <from phase0_output, primary input for weight name mapping>  # NEW — replaces model_spec_path as primary
```
Keep `model_spec_path` for backward compatibility but add a comment: "# legacy fallback when bridge_mapping absent".

**2. In the `artifacts:` section**, add after `generated_files`:
```yaml
  generated_megatron_files: []  # NEW — Megatron files generated by Phase 1 that affect convert_checkpoint
```

**3. In the `checks:` section**, add after existing checks:
```yaml
  bridge_mapping_consumed: true  # NEW — true when bridge_mapping was used as primary input
```

**4. In the validator section `details` (when present in the schema)**, add:
```yaml
    bridge_mapping_consumed: true  # NEW — mirrors checks.bridge_mapping_consumed
```

Do NOT remove any existing fields. The schema is additive — new fields are added alongside existing ones for backward compatibility.
  </action>
  <verify>
    grep -q "bridge_mapping_path" skills/adapt/references/phases/phase2/phase2_output_schema.yaml && \
    grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase2/phase2_output_schema.yaml && \
    grep -q "generated_megatron_files" skills/adapt/references/phases/phase2/phase2_output_schema.yaml && \
    grep -c "bridge_mapping" skills/adapt/references/phases/phase2/phase2_output_schema.yaml | xargs -I{} test {} -ge 3
  </verify>
  <done>
phase2_output_schema.yaml extended with source.bridge_mapping_path, checks.bridge_mapping_consumed, artifacts.generated_megatron_files. All existing fields preserved for backward compatibility.
  </done>
</task>

</tasks>

<verification>
1. agent.md contains bridge_mapping_path as primary input with model_spec_path as legacy fallback
2. agent.md Input Contract reads generated_loongforge_files and generated_megatron_files from Phase 1 output
3. agent.md Step 0 reads bridge_mapping.conversion_requirements instead of model_spec.conversion_requirements + reference_contract_path
4. agent.md Step 1 uses bridge_mapping.component_bridge[].weight_map as authoritative name map
5. agent.md references bridge_mapping_consumed in output contract
6. phase2_output_schema.yaml contains source.bridge_mapping_path, checks.bridge_mapping_consumed, artifacts.generated_megatron_files
7. No existing fields removed from either file (backward compatibility preserved)
</verification>

<success_criteria>
Phase 2 agent.md uses bridge_mapping_path as primary input for weight name mapping with model_spec_path as legacy fallback. Phase 2 consumes dual-repo file lists from Phase 1 output. Phase 2 Step 0 reads bridge_mapping.conversion_requirements. phase2_output_schema.yaml includes bridge_mapping_consumed and dual-repo fields.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase2-3-redesign/08-01-SUMMARY.md`
</output>
