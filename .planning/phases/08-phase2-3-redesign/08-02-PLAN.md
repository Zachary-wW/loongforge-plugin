---
phase: 08-phase2-3-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/references/phases/phase3/agent.md
  - skills/adapt/references/phases/phase3/phase3_output_schema.yaml
autonomous: true
requirements: [P3R-01, P3R-02]

must_haves:
  truths:
    - "Phase 3 agent.md replaces reference_contract_path with bridge_mapping_path as primary input"
    - "Phase 3 Step 0 reads bridge_mapping.implementation_contract and bridge_mapping.conversion_requirements instead of reference_contract"
    - "Phase 3 Step 2 reads bridge_mapping.phase3_reference_requirements for allowed_reference_types and custom_reference_loader_required"
    - "Phase 3 output schema adds source.bridge_mapping_path and checks.bridge_mapping_consumed"
  artifacts:
    - path: "skills/adapt/references/phases/phase3/agent.md"
      provides: "Phase 3 agent manual with bridge-mapping-first consumption"
      min_lines: 350
    - path: "skills/adapt/references/phases/phase3/phase3_output_schema.yaml"
      provides: "Extended Phase 3 output schema with bridge_mapping_consumed and bridge_mapping_path"
      contains: "bridge_mapping_consumed"
  key_links:
    - from: "skills/adapt/references/phases/phase3/agent.md"
      to: "bridge_mapping.yaml"
      via: "Step 0 reads bridge_mapping.implementation_contract and conversion_requirements"
      pattern: "bridge_mapping_path"
    - from: "skills/adapt/references/phases/phase3/agent.md"
      to: "bridge_mapping.phase3_reference_requirements"
      via: "Step 2 reads allowed_reference_types and custom_reference_loader_required from bridge_mapping"
      pattern: "phase3_reference_requirements"
    - from: "skills/adapt/references/phases/phase3/agent.md"
      to: "phase3_output_schema.yaml"
      via: "Output Contract references schema with bridge_mapping_consumed field"
      pattern: "bridge_mapping_consumed"
---

<objective>
Rewrite Phase 3 agent.md to replace reference_contract_path with bridge_mapping_path as primary input, migrate Step 0 and Step 2 to read bridge_mapping fields instead of deprecated reference_contract/model_spec fields, and update phase3_output_schema.yaml with bridge_mapping_consumed and bridge_mapping_path fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-phase2-3-redesign/08-CONTEXT.md

@skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml
@skills/adapt/references/phases/phase3/agent.md
@skills/adapt/references/phases/phase3/phase3_output_schema.yaml
@skills/adapt/lib/schema.py
</context>

<tasks>

<task type="auto">
  <name>Rewrite Phase 3 agent.md with bridge-mapping-first consumption replacing reference_contract_path</name>
  <files>skills/adapt/references/phases/phase3/agent.md</files>
  <read_first>
  - skills/adapt/references/phases/phase3/agent.md — current Phase 3 agent manual (413 lines)
  - .planning/phases/08-phase2-3-redesign/08-CONTEXT.md — decisions D-04, D-05
  - skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml — bridge_mapping schema with implementation_contract, conversion_requirements, phase3_reference_requirements
  </read_first>
  <action>
REWRITE the agent.md file. The rewrite preserves the general structure (Role, Input Contract, Loop Engineering Hooks, State Machine, Prerequisites, Steps 0-9, Output Contract, Error Handling) but makes these specific changes per decisions D-04 and D-05:

**1. Input Contract section -- add bridge_mapping_path as primary (per D-04):**

Add a new row to the "Key Fields Used" table:
```
| `run_dir/phases/phase0_output.yml` | Yes | `artifacts.bridge_mapping_path` (primary), `artifacts.model_spec_path` (legacy fallback), `artifacts.reference_contract_path` (deprecated) |
```

Add after the existing Input Contract table: "Bridge mapping consumption rule: `bridge_mapping_path` is the PRIMARY input for Phase 3 contract and reference requirements. `reference_contract_path` is DEPRECATED — its fields have been absorbed into bridge_mapping.yaml (per Phase 6 D-05: implementation_contract, conversion_requirements, phase3_reference_requirements, references). When bridge_mapping_path is absent, fall back to reference_contract_path and model_spec_path for legacy compatibility."

**2. Step 0 -- Contract and artifact preflight (per D-04):**

Replace the current Step 0 text that reads:
- `phase0_output.artifacts.reference_contract_path` when present
- `phase0_output.artifacts.model_spec_path`
- `model_spec.phase3_reference_requirements`

With:
"Read `bridge_mapping_path` as primary input for contract and requirements. When bridge_mapping.yaml is present, extract:
- `bridge_mapping.implementation_contract` (replaces reference_contract.implementation_contract)
- `bridge_mapping.conversion_requirements` (replaces reference_contract.conversion_requirements)
- `bridge_mapping.phase3_reference_requirements` (replaces model_spec.phase3_reference_requirements)
- `bridge_mapping.validator_requirements` (Phase 0 declared verification requirements for Phase 3)

When bridge_mapping_path is absent, fall back to reading `reference_contract_path` and `model_spec_path` for legacy compatibility."

Preflight checks update:
- Check 2: Replace `model_spec.conversion_requirements.must_emit_target_checkpoint` with `bridge_mapping.conversion_requirements.must_emit_target_checkpoint` (or legacy fallback).
- Check 3: Replace `phase3_reference_requirements.custom_reference_loader_required` with `bridge_mapping.phase3_reference_requirements.custom_reference_loader_required` (or legacy fallback).

**3. Step 2 -- Detect reference type (per D-05):**

Replace the current Step 2 text that reads `phase3_reference_requirements.allowed_reference_types` with:
"Read `bridge_mapping.phase3_reference_requirements.allowed_reference_types` when bridge_mapping is present. When absent, fall back to `model_spec.phase3_reference_requirements.allowed_reference_types`."

Replace the Step 2 contract application text: "reference_type must be allowed by `bridge_mapping.phase3_reference_requirements.allowed_reference_types` when the field exists (or by `model_spec.phase3_reference_requirements.allowed_reference_types` as legacy fallback)."

Replace the Step 2 HF reference inspection text: "Before accepting the default HF reference path, inspect bridge_mapping (or model_spec as fallback). If `bridge_mapping.phase3_reference_requirements.custom_reference_loader_required` is true, or if `bridge_mapping.component_bridge[].behavioral_diff` contains reference-loading behavior types..."

**4. Steps 4-7 -- loss-diff parameter mapping:**

In the parameter table for loss-diff invocation, replace:
```
reference_contract_path <- phase0_output.artifacts.reference_contract_path when present
```
With:
```
bridge_mapping_path <- phase0_output.artifacts.bridge_mapping_path when present
reference_contract_path <- phase0_output.artifacts.reference_contract_path when present (legacy fallback)
```

And add to the parameters passed to loss-diff:
```
implementation_contract <- bridge_mapping.implementation_contract when present (or reference_contract.implementation_contract as legacy fallback)
```

**5. Output Contract section (per D-07):**

Add note in the output contract: "When bridge_mapping was used as primary input, set `checks.bridge_mapping_consumed: true` in phase3_output.yml. When legacy reference_contract_path was used instead, set `checks.bridge_mapping_consumed: false` or omit the field."

**6. Keep unchanged:** State Machine, Loop Engineering Hooks, Prerequisites, Steps 1/2S/3/8/9, Error Handling. The loss-diff workflow itself (sub-doc invocation) is unchanged; only the input parameter sources change.
  </action>
  <verify>
    grep -c "bridge_mapping_path" skills/adapt/references/phases/phase3/agent.md | xargs -I{} test {} -ge 5 && \
    grep -q "reference_contract_path.*deprecated\|DEPRECATED.*reference_contract\|reference_contract_path.*legacy" skills/adapt/references/phases/phase3/agent.md && \
    grep -q "bridge_mapping.implementation_contract" skills/adapt/references/phases/phase3/agent.md && \
    grep -q "bridge_mapping.conversion_requirements" skills/adapt/references/phases/phase3/agent.md && \
    grep -q "bridge_mapping.phase3_reference_requirements" skills/adapt/references/phases/phase3/agent.md && \
    grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase3/agent.md && \
    grep -q "legacy.*fallback\|fallback.*legacy" skills/adapt/references/phases/phase3/agent.md
  </verify>
  <done>
Phase 3 agent.md rewritten with bridge_mapping_path as primary input replacing reference_contract_path, Step 0 reads bridge_mapping.implementation_contract/conversion_requirements/phase3_reference_requirements, Step 2 reads bridge_mapping.phase3_reference_requirements for reference type and custom loader, Steps 4-7 pass bridge_mapping_path to loss-diff, output contract includes bridge_mapping_consumed field. Legacy fallback paths preserved for backward compatibility.
  </done>
</task>

<task type="auto">
  <name>Update phase3_output_schema.yaml with bridge_mapping_consumed and bridge_mapping_path</name>
  <files>skills/adapt/references/phases/phase3/phase3_output_schema.yaml</files>
  <read_first>
  - skills/adapt/references/phases/phase3/phase3_output_schema.yaml — current schema (137 lines)
  - .planning/phases/08-phase2-3-redesign/08-CONTEXT.md — decisions D-07
  - skills/adapt/references/phases/phase1/phase1_output_schema.yaml — Phase 1 pattern for bridge_mapping_consumed
  </read_first>
  <action>
Update phase3_output_schema.yaml with these specific additions per D-07:

**1. In the `source:` section**, add after `hf_ckpt_path`:
```yaml
  bridge_mapping_path: "<from phase0_output — primary input for contract and reference requirements>"  # NEW
```

**2. In the `checks:` section**, add after existing checks:
```yaml
  bridge_mapping_consumed: true  # NEW — true when bridge_mapping was used as primary input
```

Add this check to BOTH mode_rules sections:
- In `mode_rules.hf_or_megatron.required_checks`, add `bridge_mapping_consumed`
- In `mode_rules.standalone.required_checks`, add `bridge_mapping_consumed`

**3. Do NOT remove `source.convert_yaml`** or any existing fields. The schema is additive.

**4. Add a deprecation note** as a comment near any reference_contract_path mention (if any exists in the schema — check first; if none, add a comment at the top):
```yaml
# DEPRECATED: reference_contract_path is absorbed into bridge_mapping.yaml (per Phase 6 D-05).
# Phase 3 reads bridge_mapping.implementation_contract, bridge_mapping.conversion_requirements,
# and bridge_mapping.phase3_reference_requirements instead.
```
  </action>
  <verify>
    grep -q "bridge_mapping_path" skills/adapt/references/phases/phase3/phase3_output_schema.yaml && \
    grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase3/phase3_output_schema.yaml && \
    grep -c "bridge_mapping" skills/adapt/references/phases/phase3/phase3_output_schema.yaml | xargs -I{} test {} -ge 3 && \
    grep -q "DEPRECATED\|deprecated" skills/adapt/references/phases/phase3/phase3_output_schema.yaml
  </verify>
  <done>
phase3_output_schema.yaml extended with source.bridge_mapping_path, checks.bridge_mapping_consumed (added to both mode_rules), deprecation note for reference_contract_path. All existing fields preserved.
  </done>
</task>

</tasks>

<verification>
1. agent.md contains bridge_mapping_path as primary input with reference_contract_path as deprecated/legacy fallback
2. agent.md Step 0 reads bridge_mapping.implementation_contract, bridge_mapping.conversion_requirements, bridge_mapping.phase3_reference_requirements
3. agent.md Step 2 reads bridge_mapping.phase3_reference_requirements for reference type and custom loader detection
4. agent.md Steps 4-7 pass bridge_mapping_path to loss-diff sub-doc
5. agent.md output contract includes bridge_mapping_consumed field
6. phase3_output_schema.yaml contains source.bridge_mapping_path and checks.bridge_mapping_consumed
7. phase3_output_schema.yaml has deprecation note for reference_contract_path
8. No existing fields removed from either file (backward compatibility preserved)
</verification>

<success_criteria>
Phase 3 agent.md uses bridge_mapping_path as primary input replacing reference_contract_path. Phase 3 Step 0 and Step 2 read bridge_mapping fields for contract and reference requirements. phase3_output_schema.yaml includes bridge_mapping_consumed and bridge_mapping_path. Legacy fallback paths preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase2-3-redesign/08-02-SUMMARY.md`
</output>
