---
phase: 09-phase4-5-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/references/phases/phase5/agent.md
  - skills/adapt/references/phases/phase5/extraction_rules.yaml
  - skills/adapt/references/phases/phase5/source_templates/llm.yaml
  - skills/adapt/references/phases/phase5/source_templates/vlm.yaml
  - skills/adapt/references/phases/phase5/source_templates/diffusion.yaml
  - skills/adapt/references/phases/phase5/phase5_output_schema.yaml
  - skills/adapt/scripts/validate_phase_completion.py
autonomous: true
requirements: [P5R-01, P5R-02]

must_haves:
  truths:
    - "Phase 5 Input Contract reads hf_analysis.yaml + bridge_mapping.yaml instead of model_spec.yaml"
    - "Phase 5 reads generated_loongforge_files + generated_megatron_files from Phase 1 output"
    - "Phase 5 extraction_rules.yaml structural_tags source is hf_analysis.yaml, not model_spec.yaml"
    - "Phase 5 extraction_rules.yaml code_paths includes megatron_code_paths categories"
    - "Phase 5 source_templates include megatron_code_paths section"
    - "Phase 5 output schema adds source.hf_analysis_path and source.bridge_mapping_path"
    - "validate_phase_completion.py has Phase 5 bridge_mapping_consumed and hf_analysis_consumed checks"
  artifacts:
    - path: "skills/adapt/references/phases/phase5/agent.md"
      provides: "Phase 5 agent manual consuming hf_analysis + bridge_mapping instead of model_spec"
      contains: "hf_analysis_path"
    - path: "skills/adapt/references/phases/phase5/extraction_rules.yaml"
      provides: "KB extraction rules reading from hf_analysis with Megatron code_paths"
      contains: "megatron_code_paths"
      min_lines: 180
    - path: "skills/adapt/references/phases/phase5/source_templates/llm.yaml"
      provides: "LLM source template with megatron_code_paths section"
      contains: "megatron_code_paths"
    - path: "skills/adapt/references/phases/phase5/source_templates/vlm.yaml"
      provides: "VLM source template with megatron_code_paths section"
      contains: "megatron_code_paths"
    - path: "skills/adapt/references/phases/phase5/source_templates/diffusion.yaml"
      provides: "Diffusion source template with megatron_code_paths section"
      contains: "megatron_code_paths"
    - path: "skills/adapt/references/phases/phase5/phase5_output_schema.yaml"
      provides: "Extended Phase 5 output schema with bridge_mapping fields"
      contains: "bridge_mapping_consumed"
  key_links:
    - from: "skills/adapt/references/phases/phase5/agent.md"
      to: "hf_analysis.yaml"
      via: "Step 1 reads hf_analysis_path for components, structural_tags, traps"
      pattern: "hf_analysis_path"
    - from: "skills/adapt/references/phases/phase5/agent.md"
      to: "bridge_mapping.yaml"
      via: "Step 1 reads bridge_mapping_path for component_bridge and gaps"
      pattern: "bridge_mapping_path"
    - from: "skills/adapt/references/phases/phase5/extraction_rules.yaml"
      to: "hf_analysis.yaml"
      via: "structural_tags and traps source changed from model_spec to hf_analysis"
      pattern: "hf_analysis"
    - from: "skills/adapt/references/phases/phase5/extraction_rules.yaml"
      to: "phase1_output.artifacts.generated_megatron_files"
      via: "megatron_code_paths match_rules source"
      pattern: "generated_megatron_files"
    - from: "skills/adapt/references/phases/phase5/source_templates/llm.yaml"
      to: "extraction_rules.yaml"
      via: "Template structure matches extraction_rules code_paths + megatron_code_paths"
      pattern: "megatron_code_paths"
---

<objective>
Rewrite Phase 5 agent.md, extraction_rules.yaml, source_templates, and phase5_output_schema.yaml to consume hf_analysis.yaml + bridge_mapping.yaml as primary input (replacing model_spec.yaml), read generated_loongforge_files + generated_megatron_files from Phase 1 output, and add Megatron file paths to KB extraction logic. Add Phase 5 checks to validate_phase_completion.py.

Purpose: Phase 5 currently reads model_spec.yaml directly for components, structural_tags, traps, and special_features. After this change, Phase 5 consumes Phase 0's three-document output and Phase 1's dual-repo file lists, enabling complete Megatron KB coverage.

Output: Updated agent.md, extraction_rules.yaml, 3 source_templates, phase5_output_schema.yaml, validate_phase_completion.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-phase4-5-redesign/09-CONTEXT.md

@skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml
@skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml
@skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml
@skills/adapt/references/phases/phase5/agent.md
@skills/adapt/references/phases/phase5/extraction_rules.yaml
@skills/adapt/references/phases/phase5/source_templates/llm.yaml
@skills/adapt/references/phases/phase5/source_templates/vlm.yaml
@skills/adapt/references/phases/phase5/source_templates/diffusion.yaml
@skills/adapt/references/phases/phase5/phase5_output_schema.yaml
@skills/adapt/scripts/validate_phase_completion.py

<interfaces>
<!-- Key types and contracts that Phase 5 needs to consume. -->

From skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml:
```yaml
model_category: llm | vlm | diffusion
candidate_family: <family_name>
components:
  <component_name>:
    diff: same | differs | new_component | absent_in_hf
    strategy: reuse_ref | adapt_ref | new_impl
    delta: []
    structural_tags: []
    hf_class: <class_name>
traps:
  - "Description of a trap or pitfall"
special_features: null  # Dict of model-specific features
novel_modules: []
fp32_modules: { strict_fp32: [], non_strict_fp32: [] }
behavior_modifications: []
weight_structure:
  total_keys: 0
  components: {}
```

From skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml:
```yaml
component_bridge:
  - hf: attention
    megatron: [MLASelfAttention]
    strategy: adapt_ref
    confidence: medium
    weight_map: [...]
    behavioral_diff:
      - topic: q_lora_rank
        impact: high
    delta: []
gaps:
  - id: G1
    component: mtp
    phase1_guidance: "..."
```

From Phase 1 output (phase1_output_schema.yaml):
```yaml
artifacts:
  generated_loongforge_files: []
  generated_megatron_files: []
checks:
  bridge_mapping_consumed: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Rewrite Phase 5 agent.md Input Contract and Step 1 for hf_analysis + bridge_mapping consumption</name>
  <files>skills/adapt/references/phases/phase5/agent.md, skills/adapt/references/phases/phase5/phase5_output_schema.yaml</files>
  <read_first>
  - skills/adapt/references/phases/phase5/agent.md — current Phase 5 agent manual (350 lines)
  - .planning/phases/09-phase4-5-redesign/09-CONTEXT.md — decisions D-04, D-09, D-10
  - skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml — hf_analysis structure
  - skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml — bridge_mapping structure
  - skills/adapt/references/phases/phase5/extraction_rules.yaml — current extraction rules (178 lines)
  - skills/adapt/references/phases/phase5/phase5_output_schema.yaml — current output schema
  </read_first>
  <action>
**Part A: Update Phase 5 agent.md (skills/adapt/references/phases/phase5/agent.md)**

The rewrite is TARGETED -- Input Contract, Step 1 data reading, and Output Contract change. Steps 2-7, State Machine, Loop Engineering Hooks, Error Handling remain unchanged unless they reference model_spec.yaml.

**1. Input Contract -- replace model_spec.yaml with hf_analysis + bridge_mapping:**

Replace the current Input Contract table with:

| Source File | Required | Key Fields Used |
|-------------|----------|-----------------|
| `run_dir/run_inputs.yml` | Yes | `source.hf_ckpt_path`, `options.model_name` |
| `run_dir/phases/phase0_output.yml` | Yes | `model.candidate_family`, `model.model_type`, `artifacts.hf_analysis_path`, `artifacts.bridge_mapping_path`, `artifacts.model_spec_path` |
| `run_dir/phases/phase0/hf_analysis.yaml` (via `phase0_output.artifacts.hf_analysis_path`) | Yes (when present) | `model_category`, `candidate_family`, `components`, `structural_tags`, `traps`, `special_features`, `behavior_modifications`, `novel_modules`, `fp32_modules`, `weight_structure` |
| `run_dir/phases/phase0/bridge_mapping.yaml` (via `phase0_output.artifacts.bridge_mapping_path`) | Yes (when present) | `component_bridge`, `gaps`, `validator_requirements` |
| `run_dir/phases/phase1_output.yml` | No | `status`, `artifacts.generated_loongforge_files`, `artifacts.generated_megatron_files` |
| `run_dir/phases/phase2_output.yml` | No | `status`, `artifacts.generated_files` |
| `run_dir/phases/phase3_output.yml` | No | `status` |
| `run_dir/phases/phase4_output.yml` | No | `status` |

Remove the old line: `Also read run_dir/phases/phase0/model_spec.yaml for components, vlm_components, traps, special_features.`

Replace with: "When `phase0_output.artifacts.hf_analysis_path` is absent (legacy Phase 0 output), fall back to `phase0_output.artifacts.model_spec_path` for components, structural_tags, traps, special_features. This fallback will be removed in a future version."

**2. Step 1 -- Update data reading instructions:**

Replace: "Also read `run_dir/phases/phase0/model_spec.yaml` for `components`, `vlm_components`, `traps`, `special_features`."

With a new paragraph:
> "Read structured data from Phase 0 v2 output:
>
> **PRIMARY sources** (when `phase0_output.artifacts.hf_analysis_path` is present):
> - From **hf_analysis.yaml**: `model_category`, `candidate_family`, `components` (key, diff, strategy, delta, structural_tags, hf_class), `traps`, `special_features`, `behavior_modifications`, `novel_modules`, `fp32_modules`
> - From **bridge_mapping.yaml**: `component_bridge` (each entry: hf, megatron, strategy, confidence, behavioral_diff, delta), `gaps` (each entry: id, component, decision, impact, phase1_guidance)
>
> **Legacy fallback** (when `phase0_output.artifacts.hf_analysis_path` is absent):
> - From **model_spec.yaml**: `candidate_family`, `model_type`, `components`, `vlm_components`, `traps`, `special_features`, `behavior_modifications`
>
> **Phase 1 file lists** (when phase1_output exists):
> - `phase1_output.artifacts.generated_loongforge_files` -- LoongForge file paths for code_paths
> - `phase1_output.artifacts.generated_megatron_files` -- Megatron file paths for megatron_code_paths (NEW)
>
> When phase1_output exists but `generated_megatron_files` is absent (legacy Phase 1 output), fall back to `generated_files` for LoongForge paths and omit megatron_code_paths."

**3. Traps extraction -- update Step 1 traps instruction:**

In Step 1, after the data reading paragraph, update the traps extraction guidance:

> "When using hf_analysis + bridge_mapping as primary sources:
> - `traps` come from `hf_analysis.traps` (string list, same format as model_spec.traps)
> - `special_features` come from `hf_analysis.special_features` (dict format, same as model_spec.special_features)
> - Additional trap-like entries can be derived from `bridge_mapping.component_bridge[].behavioral_diff`: each behavioral_diff entry with `impact: high` or `impact: critical` becomes a trap entry with `field=topic` and `detail="HF: <hf> / Megatron: <megatron>"`.
> - Deduplication: if a behavioral_diff topic overlaps with an existing trap `field`, prefer the existing trap entry.
>
> When using model_spec.yaml as legacy fallback: traps extraction is unchanged from current behavior."

**4. Step 1 completion condition -- update:**

Replace: "Step 1 is complete only when all fields required by the selected source template are either populated from Phase outputs/model_spec or explicitly represented by the placeholder rules from `extraction_rules.yaml`."

With: "Step 1 is complete only when all fields required by the selected source template are either populated from Phase outputs/hf_analysis/bridge_mapping or explicitly represented by the placeholder rules from `extraction_rules.yaml`."

**5. Output Contract -- add new source and checks fields:**

In the Output Contract section, update:

```yaml
source:
  hf_ckpt_path: "<run_inputs.source.hf_ckpt_path>"
  hf_analysis_path: "<phase0_output.artifacts.hf_analysis_path>"     # NEW (v2) -- when present
  bridge_mapping_path: "<phase0_output.artifacts.bridge_mapping_path>"  # NEW (v2) -- when present

checks:
  source_yaml_exists: true
  index_entry_exists: true
  log_entry_exists: true
  required_sections_present: true
  placeholders_consistent_with_phase_status: true
  bridge_mapping_consumed: true   # NEW (v2) -- true when bridge_mapping was read; absent for legacy
  hf_analysis_consumed: true      # NEW (v2) -- true when hf_analysis was read; absent for legacy
```

**6. Step 7 kb-consistency check -- add megatron_code_paths verification:**

In Step 7 pass conditions, add:
> "- If Phase 1 status is `passed`, `megatron_code_paths` section must exist in the source YAML (in addition to `code_paths`)"

***

**Part B: Update phase5_output_schema.yaml (skills/adapt/references/phases/phase5/phase5_output_schema.yaml)**

Add these new fields:

1. In the `source:` section, add:
```yaml
  hf_analysis_path: "<phase0_output.artifacts.hf_analysis_path>"     # NEW (v2) -- present when Phase 0 v2 output exists
  bridge_mapping_path: "<phase0_output.artifacts.bridge_mapping_path>"  # NEW (v2) -- present when Phase 0 v2 output exists
```

2. In the `checks:` section, add:
```yaml
  bridge_mapping_consumed: true   # NEW (v2) -- true when bridge_mapping.yaml was read; absent for legacy runs
  hf_analysis_consumed: true      # NEW (v2) -- true when hf_analysis.yaml was read; absent for legacy runs
```

3. In the `artifacts:` section, add a comment:
```yaml
  # When Phase 1 v2 output exists, source YAML includes megatron_code_paths section
```

4. Add a version comment at the top:
```yaml
# v2 additions: source.hf_analysis_path, source.bridge_mapping_path,
# checks.bridge_mapping_consumed, checks.hf_analysis_consumed
# These fields are present only when Phase 0 v2 output (three-document) is available.
```
  </action>
  <verify>
    grep -q "hf_analysis_path" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "bridge_mapping_path" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "model_spec.*legacy\|legacy.*model_spec\|legacy fallback" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "generated_loongforge_files" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "generated_megatron_files" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "behavioral_diff" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "megatron_code_paths" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "hf_analysis_consumed" skills/adapt/references/phases/phase5/agent.md && \
    grep -q "hf_analysis_path" skills/adapt/references/phases/phase5/phase5_output_schema.yaml && \
    grep -q "bridge_mapping_path" skills/adapt/references/phases/phase5/phase5_output_schema.yaml && \
    grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase5/phase5_output_schema.yaml && \
    grep -q "hf_analysis_consumed" skills/adapt/references/phases/phase5/phase5_output_schema.yaml
  </verify>
  <done>
Phase 5 agent.md Input Contract reads hf_analysis.yaml + bridge_mapping.yaml as primary sources (model_spec.yaml legacy fallback). Step 1 reads components/structural_tags/traps from hf_analysis, component_bridge/gaps from bridge_mapping, generated_loongforge_files + generated_megatron_files from Phase 1. Traps extraction includes bridge_mapping behavioral_diff entries. Step 7 kb-consistency checks megatron_code_paths. Output Contract adds source.hf_analysis_path, source.bridge_mapping_path, checks.bridge_mapping_consumed, checks.hf_analysis_consumed. phase5_output_schema.yaml updated with same fields.
  </done>
</task>

<task type="auto">
  <name>Update extraction_rules.yaml for hf_analysis source + Megatron code_paths; update source_templates</name>
  <files>skills/adapt/references/phases/phase5/extraction_rules.yaml, skills/adapt/references/phases/phase5/source_templates/llm.yaml, skills/adapt/references/phases/phase5/source_templates/vlm.yaml, skills/adapt/references/phases/phase5/source_templates/diffusion.yaml, skills/adapt/scripts/validate_phase_completion.py</files>
  <read_first>
  - skills/adapt/references/phases/phase5/extraction_rules.yaml — current extraction rules (178 lines)
  - .planning/phases/09-phase4-5-redesign/09-CONTEXT.md — decisions D-05, D-06, D-07, D-08, D-09, D-12
  - skills/adapt/references/phases/phase5/source_templates/llm.yaml — current LLM template
  - skills/adapt/references/phases/phase5/source_templates/vlm.yaml — current VLM template
  - skills/adapt/references/phases/phase5/source_templates/diffusion.yaml — current Diffusion template
  - skills/adapt/scripts/validate_phase_completion.py — current validator (needs Phase 5 checks)
  </read_first>
  <action>
**Part A: Update extraction_rules.yaml (skills/adapt/references/phases/phase5/extraction_rules.yaml)**

The extraction_rules.yaml is the core data mapping file. Changes are additive + source path updates.

**1. Version bump:**
Change `version: 1` to `version: 2`.

Add a `version_history` comment:
```yaml
# version: 2 — structural_tags source changed from model_spec.yaml to hf_analysis.yaml;
#              code_paths source reads generated_loongforge_files + generated_megatron_files;
#              added megatron_code_paths section; traps source includes bridge_mapping behavioral_diff.
```

**2. Update purpose:**
Change purpose to: "Extract reusable model-family knowledge from Phase 0-4 outputs, hf_analysis.yaml, and bridge_mapping.yaml into knowledge_base/sources/<model_cat>/<family>.yaml, INDEX.md, and LOG.md."

**3. Update base_fields:**
- Change `phase1_files: phase1_output.artifacts.generated_files when phase1_output exists` to:
  ```yaml
  phase1_loongforge_files: phase1_output.artifacts.generated_loongforge_files when phase1_output exists
  phase1_megatron_files: phase1_output.artifacts.generated_megatron_files when phase1_output exists
  phase1_files: phase1_output.artifacts.generated_files when phase1_output exists  # legacy fallback when split lists absent
  ```

**4. Update structural_tags section:**
Change `source: run_dir/phases/phase0/model_spec.yaml` to `source: run_dir/phases/phase0/hf_analysis.yaml (primary) or run_dir/phases/phase0/model_spec.yaml (legacy fallback)`.

Update each tag's true_when rules to reference hf_analysis paths:
- `has_mla`: `hf_analysis.components.attention.structural_tags contains mla` (unchanged logic, source changed)
- `has_moe`: `hf_analysis.components contains moe_gate or moe_layer` (unchanged logic)
- `has_qk_layernorm`: `hf_analysis.components.attention.structural_tags contains q_norm or qk_norm or qk_layernorm` (unchanged logic)
- `has_linear_attn`: unchanged
- `has_mtp`: unchanged
- `is_mha`: unchanged
- `vocab_size_tier`: unchanged (reads from config.json, not model_spec or hf_analysis)

Add a note at the end of structural_tags section:
> "When hf_analysis.yaml is absent (legacy Phase 0 output), fall back to reading from model_spec.yaml using the same true_when rules applied to model_spec.components[].structural_tags."

**5. Update diff_components section:**
Add: "When bridge_mapping.yaml is present, also check bridge_mapping.component_bridge[].delta for additional diff items."

**6. Update traps section:**
Change `source_sections` to:
```yaml
  source_sections:
    - hf_analysis.traps                          # PRIMARY (v2)
    - bridge_mapping.component_bridge[].behavioral_diff  # NEW (v2) -- high/critical impact entries become trap entries
    - model_spec.traps                            # LEGACY fallback
    - model_spec.special_features                 # LEGACY fallback
```

Add a `behavioral_diff_rule` subsection:
```yaml
  behavioral_diff_rule:
    condition: behavioral_diff.impact in [high, critical]
    field: behavioral_diff.topic
    detail: "HF: <behavioral_diff.hf> / Megatron: <behavioral_diff.megatron>"
    deduplication: skip when field matches an existing trap entry field
```

**7. Add megatron_code_paths section (after code_paths):**

Insert after the `omni_reference` section and before `append_flow_rules`:

```yaml
megatron_code_paths:
  only_when: phase1_megatron_files is not empty
  placeholder_when_missing: "# Phase 1 not complete or no Megatron files generated, to be supplemented"
  categories:
    llm:
      source: phase1_megatron_files
      match_rules:
        - pattern: "megatron/core/transformer/*.py"
          field: transformer_core
        - pattern: "megatron/core/transformer/custom/*.py"
          field: custom
        - pattern: "megatron/models/*.py"
          field: models
      fallback_rule: >
        Other .py files under megatron/ use the subdirectory path as the field name.
        For example, megatron/core/transformer/experimental_attention_variant/dsa.py
        maps to field: experimental_attention_variant_dsa.
      fallback_example:
        experimental_attention_variant_dsa: megatron/core/transformer/experimental_attention_variant/dsa.py
    vlm:
      inherits: llm
      # VLM-specific Megatron paths follow the same rules as LLM
    diffusion:
      inherits: llm
      # Diffusion-specific Megatron paths follow the same rules as LLM
```

**8. Update append_flow_rules:**

Add new rules:
```yaml
  - if megatron_code_paths section does not exist and phase1_megatron_files is not empty, insert complete megatron_code_paths after code_paths using the model-category template.
```

**9. Update source_templates reference:**

No change needed -- the source_templates already reference the correct file paths.

***

**Part B: Update source templates (3 files)**

**llm.yaml:**

Add after the `code_paths:` section and before the `traps:` section:

```yaml
megatron_code_paths:
  # Paths to Megatron-side files generated or modified during adaptation.
  # Only populated when Phase 1 generated Megatron files.
  <field>: <megatron_path or # Phase 1 not complete, to be supplemented>
  # Example:
  # experimental_attention_variant_dsa: megatron/core/transformer/experimental_attention_variant/dsa.py
```

**vlm.yaml:**

Same megatron_code_paths section as llm.yaml, inserted after code_paths (which includes the encoder field) and before traps.

**diffusion.yaml:**

Same megatron_code_paths section as llm.yaml, inserted after code_paths and before traps.

***

**Part C: Add Phase 5 checks to validate_phase_completion.py**

In the `validate_phase_output` function, after the existing `if phase == 4:` block (added by Plan 01) and before `_validate_loop_evidence(data)`, add:

```python
    if phase == 5:
        checks = data.get("checks", {})

        # Bridge mapping consumption check (conditional for backward compat)
        bridge_mapping_consumed = checks.get("bridge_mapping_consumed")
        if bridge_mapping_consumed is not None:
            _expect(bridge_mapping_consumed is True,
                    "checks.bridge_mapping_consumed must be true when bridge_mapping was used as input")
            artifacts = data.get("artifacts", {})
            bm_path = artifacts.get("bridge_mapping_path")
            if bm_path is None:
                # Also check source section
                bm_path = data.get("source", {}).get("bridge_mapping_path")
            if bm_path is not None:
                full_path = run_dir / bm_path
                if not full_path.exists():
                    raise ValueError(f"checks.bridge_mapping_consumed is true but bridge_mapping file not found: {full_path}")
                bm_data = yaml.safe_load(full_path.read_text())
                if not isinstance(bm_data, dict):
                    raise ValueError(f"bridge_mapping.yaml is not a valid YAML dict: {full_path}")
                component_bridge = bm_data.get("component_bridge")
                _expect(isinstance(component_bridge, list) and len(component_bridge) > 0,
                        "bridge_mapping.yaml component_bridge must be a non-empty list when bridge_mapping_consumed is true")

        # HF analysis consumption check (conditional for backward compat)
        hf_analysis_consumed = checks.get("hf_analysis_consumed")
        if hf_analysis_consumed is not None:
            _expect(hf_analysis_consumed is True,
                    "checks.hf_analysis_consumed must be true when hf_analysis was used as input")
            source = data.get("source", {})
            hfa_path = source.get("hf_analysis_path")
            if hfa_path is not None:
                full_path = run_dir / hfa_path
                if not full_path.exists():
                    raise ValueError(f"source.hf_analysis_path references a file that does not exist: {full_path}")
                hfa_data = yaml.safe_load(full_path.read_text())
                if not isinstance(hfa_data, dict):
                    raise ValueError(f"hf_analysis.yaml is not a valid YAML dict: {full_path}")
                _expect(isinstance(hfa_data.get("model_category"), str),
                        "hf_analysis.yaml model_category must be a string")
                _expect(isinstance(hfa_data.get("components"), dict) and len(hfa_data["components"]) > 0,
                        "hf_analysis.yaml components must be a non-empty dict")
```
  </action>
  <verify>
    grep -q "version: 2" skills/adapt/references/phases/phase5/extraction_rules.yaml && \
    grep -q "hf_analysis.yaml" skills/adapt/references/phases/phase5/extraction_rules.yaml && \
    grep -q "megatron_code_paths:" skills/adapt/references/phases/phase5/extraction_rules.yaml && \
    grep -q "generated_megatron_files" skills/adapt/references/phases/phase5/extraction_rules.yaml && \
    grep -q "behavioral_diff_rule:" skills/adapt/references/phases/phase5/extraction_rules.yaml && \
    grep -q "megatron_code_paths:" skills/adapt/references/phases/phase5/source_templates/llm.yaml && \
    grep -q "megatron_code_paths:" skills/adapt/references/phases/phase5/source_templates/vlm.yaml && \
    grep -q "megatron_code_paths:" skills/adapt/references/phases/phase5/source_templates/diffusion.yaml && \
    grep -q "phase == 5" skills/adapt/scripts/validate_phase_completion.py && \
    grep -q "bridge_mapping_consumed" skills/adapt/scripts/validate_phase_completion.py && \
    grep -q "hf_analysis_consumed" skills/adapt/scripts/validate_phase_completion.py && \
    python3 -m py_compile skills/adapt/scripts/validate_phase_completion.py
  </verify>
  <done>
extraction_rules.yaml version bumped to 2 with hf_analysis source for structural_tags, megatron_code_paths section with match rules for LLM/VLM/Diffusion, behavioral_diff_rule for traps from bridge_mapping, generated_loongforge_files + generated_megatron_files base fields. Source templates (llm.yaml, vlm.yaml, diffusion.yaml) have megatron_code_paths section after code_paths. validate_phase_completion.py has Phase 5 conditional checks for bridge_mapping_consumed and hf_analysis_consumed.
  </done>
</task>

</tasks>

<verification>
1. agent.md Input Contract lists hf_analysis.yaml and bridge_mapping.yaml as primary sources
2. agent.md Step 1 reads generated_loongforge_files + generated_megatron_files from Phase 1
3. agent.md mentions model_spec.yaml as legacy fallback only
4. agent.md traps extraction includes bridge_mapping behavioral_diff entries
5. agent.md Step 7 checks megatron_code_paths section
6. extraction_rules.yaml version is 2 with hf_analysis source for structural_tags
7. extraction_rules.yaml has megatron_code_paths section with match rules
8. All 3 source templates have megatron_code_paths section
9. phase5_output_schema.yaml has source.hf_analysis_path, source.bridge_mapping_path, checks.bridge_mapping_consumed, checks.hf_analysis_consumed
10. validate_phase_completion.py has Phase 5 conditional checks
</verification>

<success_criteria>
1. `grep -q "hf_analysis_path" skills/adapt/references/phases/phase5/agent.md` passes
2. `grep -q "generated_megatron_files" skills/adapt/references/phases/phase5/agent.md` passes
3. `grep -q "megatron_code_paths" skills/adapt/references/phases/phase5/extraction_rules.yaml` passes
4. `grep -q "megatron_code_paths" skills/adapt/references/phases/phase5/source_templates/llm.yaml` passes
5. `grep -q "bridge_mapping_consumed" skills/adapt/references/phases/phase5/phase5_output_schema.yaml` passes
6. `grep -q "phase == 5" skills/adapt/scripts/validate_phase_completion.py` passes
7. `python3 -m py_compile skills/adapt/scripts/validate_phase_completion.py` passes
8. `python3 -m pytest skills/adapt/tests/ -x -q --timeout=30` still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase4-5-redesign/09-02-SUMMARY.md`
</output>
