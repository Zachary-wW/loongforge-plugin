---
phase: 06-phase0-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/lib/schema.py
  - skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml
  - skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml
  - skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml
  - skills/adapt/references/phases/phase0/agent.md
  - skills/adapt/references/phases/phase0/phase0_output_schema.yaml
autonomous: true
requirements: [P0R-01, P0R-03, P0R-04, P0R-05, P0R-06, P0R-09]

must_haves:
  truths:
    - "Pydantic v2 models for HfAnalysis, ReferenceImplAnalysis, and BridgeMapping parse valid YAML and reject invalid input"
    - "Phase 0 agent.md defines a 9-step state machine with quality inner loop and bridge step"
    - "phase0_output_schema.yaml references all three new output artifacts"
    - "bridge_mapping_schema.yaml structure matches D-16 spec with component_bridge, gaps, validator_requirements, and absorbed reference_contract fields (per D-05)"
  artifacts:
    - path: "skills/adapt/lib/schema.py"
      provides: "Pydantic v2 models: HfAnalysis, ReferenceImplAnalysis, BridgeMapping, Phase0OutputV2"
      contains: "class HfAnalysis"
    - path: "skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml"
      provides: "YAML schema template for hf_analysis.yaml"
      contains: "model_category"
    - path: "skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml"
      provides: "YAML schema template for reference_impl_analysis.yaml"
      contains: "megatron_family"
    - path: "skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml"
      provides: "YAML schema template for bridge_mapping.yaml"
      contains: "component_bridge"
    - path: "skills/adapt/references/phases/phase0/agent.md"
      provides: "Phase 0 agent state machine and execution steps"
      min_lines: 400
    - path: "skills/adapt/references/phases/phase0/phase0_output_schema.yaml"
      provides: "Phase 0 output schema template"
      contains: "hf_analysis_path"
  key_links:
    - from: "skills/adapt/lib/schema.py"
      to: "skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml"
      via: "BridgeMapping model matches YAML schema fields"
      pattern: "class BridgeMapping"
    - from: "skills/adapt/references/phases/phase0/agent.md"
      to: "skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml"
      via: "Step 5 bridge step references bridge_mapping schema"
      pattern: "bridge_mapping"
    - from: "skills/adapt/references/phases/phase0/phase0_output_schema.yaml"
      to: "skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml"
      via: "artifacts section references hf_analysis_path"
      pattern: "hf_analysis_path"
---

<objective>
Define the three-document output schema contracts (Pydantic v2 models + YAML schema templates) and rewrite the Phase 0 agent manual to reflect the new dual-reference analysis workflow.

Purpose: These schemas and the agent manual are the foundational contracts that all downstream work (Megatron analyzer, bridge step, validation, Phase 1/2 consumer updates) depends on. Without them, no other plan can proceed correctly.

Output: 6 files — 3 new YAML schema templates, updated schema.py, rewritten agent.md, new phase0_output_schema.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-phase0-redesign/06-CONTEXT.md
@skills/adapt/knowledge_base/examples/model_spec_llm.yaml
@skills/adapt/knowledge_base/schema/HF_SCAN_RULES.md
@skills/adapt/references/phases/phase0/reference_contract_schema.yaml

<interfaces>
<!-- Current schema.py exports that Phase 0 redesign extends -->

From skills/adapt/lib/schema.py:
```python
class RunInputs(BaseModel):
    """Top-level run_inputs.yml. v1 had source/paths/options only.
    v2 adds optional repos and loop."""
    source: SourceBlock = Field(default_factory=SourceBlock)
    paths: PathsBlock = Field(default_factory=PathsBlock)
    options: OptionsBlock = Field(default_factory=OptionsBlock)
    repos: Optional[ReposBlock] = None
    loop: Optional[LoopBudget] = None
```

From skills/adapt/scripts/validate_phase_completion.py:
```python
def validate_phase_output(run_dir: Path, phase: int) -> None:
    # phase == 0: checks model_spec_exists, components_non_empty,
    #             weight_structure_non_empty, slice_hf_ckpt_path_resolved
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Pydantic v2 models and YAML schema templates for three-document output</name>
  <files>skills/adapt/lib/schema.py, skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml, skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml, skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml</files>

  <read_first>
    - skills/adapt/lib/schema.py (current Pydantic models, follow same pattern: BaseModel, ConfigDict(extra='forbid'), Field with validators)
    - skills/adapt/knowledge_base/examples/model_spec_llm.yaml (current model_spec structure being superseded — every field here must have a home in the new HfAnalysis model)
    - skills/adapt/references/phases/phase0/reference_contract_schema.yaml (being absorbed into bridge_mapping — every field here must be representable in the new BridgeMapping model)
    - .planning/phases/06-phase0-redesign/06-CONTEXT.md (D-01 through D-11, D-16 define the exact schema structure requirements)
  </read_first>

  <behavior>
    - Test 1: HfAnalysis parses the current model_spec_llm.yaml content (all existing fields preserved)
    - Test 2: ReferenceImplAnalysis parses a Megatron-side analysis with class_signatures, init_members, forward_flow, config_fields
    - Test 3: BridgeMapping parses a complete component_bridge entry with weight_map entries and gaps
    - Test 4: BridgeMapping rejects a gap entry missing required field 'impact'
    - Test 5: HfAnalysis rejects unknown extra fields (extra='forbid')
  </behavior>

  <action>
    **1. Extend skills/adapt/lib/schema.py** — Add the following Pydantic v2 model classes after the existing models. Follow the same pattern: BaseModel, ConfigDict(extra='forbid'), Field with validators, @model_validator for cross-field checks.

    **HfAnalysis** (supersedes model_spec.yaml — per D-01, D-04):
    - Top-level fields: `model_category: Literal["llm","vlm","diffusion"]`, `candidate_family: str`, `hf_reference_path: str`, `candidate_match_reason: str`, `has_chat_template: bool = False`, `low_confidence_candidate: Optional[bool] = None`, `low_confidence_reason: Optional[str] = None`
    - `components: Dict[str, ComponentAnalysis]` where ComponentAnalysis has: `diff: Literal["same","differs","new_component","absent_in_hf"]`, `strategy: Literal["reuse_ref","adapt_ref","new_impl"]`, `delta: List[str] = []`, `note: Optional[str] = None`, `hf_class: Optional[str]`, `hf_file: Optional[str]`, `hf_line: Optional[int]`, `structural_tags: List[str] = []`, `same_class_as: Optional[str] = None`
    - `novel_modules: List[NovelModule]` where NovelModule has: `hf_class: str`, `hf_file: Optional[str]`, `hf_line: Optional[int]`, `desc: str`, `external_dependency: bool = False`, `sub_modules: List[str] = []`, `key_params: List[str] = []`
    - `fp32_modules: Fp32Modules` (strict_fp32 + non_strict_fp32 lists)
    - `generation_constraints: List[str] = []`
    - `config_precision_delta: List[str] = []`
    - `behavior_modifications: List[BehaviorModification] = []` where BehaviorModification has: `id: str`, `component: str`, `behavior_type: str`, `source_evidence: SourceEvidence` (hf_file, hf_line, config_fields), `required_behavior: str`, `affected_existing_modules: List[str] = []`, `validation_hint: str = ""`
    - `traps: List[str] = []`
    - `special_features: Optional[Dict[str, Any]] = None`
    - `weight_structure: Optional[WeightStructure] = None` — keep this here for backward compat; Phase 0 Step 4 appends it to hf_analysis.yaml (same as current model_spec.yaml Step 4)

    **ReferenceImplAnalysis** (NEW — per D-06, D-18):
    - Top-level fields: `megatron_family: str`, `source_repo: str`, `source_ref: str = "main"`, `analysis_timestamp: str`
    - `modules: Dict[str, MegatronModuleAnalysis]` where MegatronModuleAnalysis has: `class_name: str`, `source_file: str`, `base_classes: List[str] = []`, `init_signature: InitSignature` (params: List[ParamEntry] where ParamEntry has name, type_hint, default_value, description), `forward_signature: ForwardSignature` (inputs: List[ParamEntry], outputs: List[str], description: str = ""), `config_fields_used: List[ConfigFieldRef] = []` where ConfigFieldRef has field_name, config_class, usage_description, `submodule_slots: List[SubmoduleSlot] = []` where SubmoduleSlot has slot_name, slot_type, default_class, is_replaceable: bool, `weight_params: List[WeightParamRef] = []` where WeightParamRef has param_name, shape_hint, dtype
    - `config_classes: Dict[str, ConfigClassAnalysis]` where ConfigClassAnalysis has: class_name, source_file, fields: List[ConfigFieldEntry] (field_name, type_hint, default_value, description), parent_classes: List[str]

    **BridgeMapping** (NEW — per D-16, D-09, D-07):
    - Top-level fields: `model: str`, `hf_source: str`, `megatron_family: str`
    - `component_bridge: List[ComponentBridge]` where ComponentBridge has:
      - `hf: str` (HF component key, e.g. "attention")
      - `megatron: Optional[List[str]]` (Megatron class/module references; None for gaps — per D-09)
      - `strategy: Literal["reuse_ref","adapt_ref","new_impl"]`
      - `confidence: Literal["high","medium","low"]`
      - `weight_map: Optional[List[WeightMapEntry]]` (None when Megatron module doesn't exist — per D-10; each entry has hf: str, megatron: str, shape_hint: str = "", reshape_required: str = "" — per D-11)
      - `behavioral_diff: List[BehavioralDiff] = []` where BehavioralDiff has: topic, hf, megatron, impact: Literal["critical","high","medium"], strategy: str
      - `delta: List[str] = []` (structural differences, inherited from current model_spec delta)
    - `gaps: List[GapEntry]` where GapEntry has: `id: str` (G1, G2...), `component: str`, `hf: str`, `megatron: str` (what exists or "NEW" — per D-07), `decision: str`, `impact: Literal["critical","high","medium"]`, `phase1_guidance: str`
    - `validator_requirements: List[str] = []` (per D-16: Phase 0 declares what downstream needs to verify)
    - Absorbed from reference_contract.yml (per D-05): `implementation_contract: Optional[Dict[str, Any]] = None`, `conversion_requirements: Optional[Dict[str, Any]] = None`, `phase3_reference_requirements: Optional[Dict[str, Any]] = None`
    - `references: List[ReferenceEntry] = []` — migrated from reference_contract.yml

    **2. Create YAML schema template files**:

    **skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml**: Mirror the HfAnalysis Pydantic model fields as a YAML template with comments. Include every field from the current model_spec_llm.yaml example, mapped to the new field names. Mark `weight_structure` as "appended by Phase 0 Step 4". Add a header comment: "# Schema template for phases/phase0/hf_analysis.yaml. Supersedes model_spec.yaml (per D-04)."

    **skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml**: Mirror the ReferenceImplAnalysis Pydantic model. Include example entries for a Megatron MLASelfAttention module (class_name, init_signature with linear_q_down_proj/linear_kv_proj params, forward_signature with hidden_states input, submodule_slots for core_attention/linear_q_proj, weight_params for query/key/value). Add header comment: "# Schema template for phases/phase0/reference_impl_analysis.yaml (per D-18)."

    **skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml**: Mirror the BridgeMapping Pydantic model. Include the full component_bridge structure from D-16 in the YAML template with example entries showing: (a) an attention component where Megatron has MLASelfAttention (weight_map populated), (b) a gap component where Megatron lacks a module (weight_map: null, gap entry with impact=critical), (c) behavioral_diff entries. Add header comment: "# Schema template for phases/phase0/bridge_mapping.yaml. Absorbs reference_contract.yml (per D-05)."
  </action>

  <verify>
    <automated>cd /Users/weizhihao/workspace/agent_skills/loongforge-plugin && python3 -c "from skills.adapt.lib.schema import HfAnalysis, ReferenceImplAnalysis, BridgeMapping; print('import OK')" && python3 -m pytest skills/adapt/tests/lib/test_schema_phase0.py -x -q 2>/dev/null || echo "test file not yet created — verify import only"</automated>
  </verify>

  <done>
    - skills/adapt/lib/schema.py contains classes HfAnalysis, ReferenceImplAnalysis, BridgeMapping with all fields matching D-01/D-06/D-09/D-16 specs
    - skills/adapt/knowledge_base/schema/hf_analysis_schema.yaml exists and contains all component fields from current model_spec_llm.yaml
    - skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml exists with MegatronModuleAnalysis structure
    - skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml exists with component_bridge, gaps, validator_requirements sections
    - All three Pydantic models use ConfigDict(extra='forbid') consistent with existing schema.py pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase 0 agent.md with dual-reference state machine and bridge step</name>
  <files>skills/adapt/references/phases/phase0/agent.md, skills/adapt/references/phases/phase0/phase0_output_schema.yaml</files>

  <read_first>
    - skills/adapt/references/phases/phase0/agent.md (current 7-step agent manual being rewritten — understand every state transition and output field)
    - skills/adapt/references/tools/hf-model-analyzer/SKILL.md (Stage 1-3 retained for HF side per D-17)
    - .planning/phases/06-phase0-redesign/06-CONTEXT.md (D-01 through D-19 define all constraints for the rewrite)
    - skills/adapt/knowledge_base/schema/STEP_GATE.md (step gate format must be preserved)
    - skills/adapt/references/phases/phase1/agent.md (Phase 1 reads Phase 0 output — understand what Phase 1 needs from bridge_mapping)
  </read_first>

  <action>
    **Rewrite skills/adapt/references/phases/phase0/agent.md** completely. The current file is 584 lines with a 7-step state machine. The new file must reflect the dual-reference architecture. Key structural changes:

    **1. Role section** — Update from "HF Full Parsing and Checkpoint Slicing" to "Dual-Reference Bridge Analysis and Checkpoint Slicing". Add: "Phase 0 produces three core deliverables: hf_analysis.yaml (HF side), reference_impl_analysis.yaml (Megatron/community side), bridge_mapping.yaml (component-by-component bridge)."

    **2. State Machine** — Replace current 8-state machine with new states:

    ```
    phase0.pending
    phase0.resolving_sources
    phase0.analyzing_hf           (unchanged — runs hf-model-analyzer)
    phase0.analyzing_megatron     (NEW — runs megatron-reference-analyzer)
    phase0.bridge_mapping         (NEW — deterministic bridge step, per D-19)
    phase0.quality_loop           (NEW — quality inner loop, per D-15)
    phase0.analyzing_wip          (adapted — reference contract extraction from bridge_mapping)
    phase0.slicing_ckpt           (unchanged)
    phase0.validating_output      (updated checks)
    phase0.passed
    phase0.human_needed
    ```

    State transitions table — add transitions for:
    - `analyzing_hf` → `analyzing_megatron` (after model_spec/hf_analysis is ready)
    - `analyzing_megatron` → `bridge_mapping` (after reference_impl_analysis is ready)
    - `bridge_mapping` → `quality_loop` (after bridge_mapping.yaml is produced)
    - `quality_loop` → `analyzing_wip` (if complete) or → `bridge_mapping` (if incomplete, dig deeper — max 3 rounds per D-15) or → `human_needed` (if still incomplete after 3 rounds)
    - `analyzing_wip` → `slicing_ckpt` (absorbs reference_contract into bridge_mapping)

    **3. Quality Inner Loop** (per D-15) — Add a dedicated section describing:
    - Phase 0 does NOT use the 12-state Loop FSM (no PRs, issues, or code changes to external repos)
    - Instead uses a quality inner loop: analyze → completeness check → (if incomplete) dig deeper → max 3 rounds → human_needed
    - Completeness checks: every component in hf_analysis has a corresponding entry in bridge_mapping (either component_bridge or gaps); weight_map entries exist for all components where Megatron has a module; no empty required fields in bridge_mapping
    - When incomplete: return to bridge_mapping state to fill gaps, NOT to analyzing_hf (HF analysis is done once)

    **4. Execution Progress Table** — Replace current 7-step table with 9-step table:

    | Step | State | Name | Status |
    |---|---|---|---|
    | 1 | resolving_sources | Resolve HF source inputs | (unchanged) |
    | 2 | analyzing_hf | Scan HF checkpoint directory | (unchanged) |
    | 3 | analyzing_hf | Invoke hf-model-analyzer → write hf_analysis.yaml | (updated output name) |
    | 4 | analyzing_hf | Extract weight structure and append to hf_analysis.yaml | (unchanged) |
    | 5 | analyzing_megatron | Invoke megatron-reference-analyzer → write reference_impl_analysis.yaml | (NEW — per D-18) |
    | 5.5 | bridge_mapping | Bridge step: combine hf_analysis + reference_impl_analysis → write bridge_mapping.yaml | (NEW — per D-19) |
    | 6 | quality_loop | Completeness check: all components mapped or gapped; max 3 rounds | (NEW — per D-15) |
    | 7 | analyzing_wip | Absorb reference_contract fields into bridge_mapping; extract implementation contract | (adapted — per D-05) |
    | 8 | slicing_ckpt | Resolve slice output | (unchanged from current Step 6) |
    | 9 | validating_output | Validate and write phase0_output.yml | (updated checks) |

    **5. Step 5 — Invoke megatron-reference-analyzer** (NEW step):
    - Read `run_inputs.yml` repos.megatron (or paths.megatron_path for local mode)
    - Read `hf_analysis.yaml` components section to know which Megatron modules to look up
    - Invoke `references/tools/megatron-reference-analyzer/SKILL.md`
    - Pass: megatron_path, component_list (from hf_analysis.components keys + candidate_family from KB), run_dir
    - Output: `phases/phase0/reference_impl_analysis.yaml`
    - If no Megatron path is available, write a minimal reference_impl_analysis.yaml with `megatron_family: unknown` and skip to bridge_mapping with gaps for all components

    **6. Step 5.5 — Bridge Step** (NEW — per D-19, deterministic):
    - Read `hf_analysis.yaml` and `reference_impl_analysis.yaml`
    - For each component in hf_analysis.components:
      - If reference_impl_analysis.modules has a matching Megatron module: create a component_bridge entry with strategy from hf_analysis, confidence based on diff field (same→high, differs→medium, new_component→low), weight_map populated from reference_impl_analysis weight_params mapped to HF weight_structure (per D-09, D-11)
      - If no matching Megatron module: create a component_bridge entry with megatron: null, weight_map: null, AND a gaps[] entry with impact level derived from component importance (per D-07, D-10)
    - Add behavioral_diff entries from hf_analysis.behavior_modifications, mapping each to the corresponding component_bridge
    - Migrate reference_contract.yml fields into bridge_mapping: implementation_contract, conversion_requirements, phase3_reference_requirements (per D-05)
    - Handle low_confidence_candidate: when hf_analysis.low_confidence_candidate is true, mark all component_bridge entries as confidence=low and add gap entries for mismatched components (per D-12)
    - Write `phases/phase0/bridge_mapping.yaml`
    - Generate `phases/phase0/gap_decisions.md` from bridge_mapping.gaps entries: for each gap entry, write a human-readable section with the gap ID, component name, HF side description, what Megatron lacks, decision rationale, and Phase 1 guidance (per D-02)

    **7. Step 6 — Quality Inner Loop** (NEW — per D-15):
    - Completeness checks:
      1. Every component in hf_analysis.components has a corresponding entry in bridge_mapping.component_bridge
      2. No component_bridge entry has both megatron=null AND weight_map non-null (contradiction)
      3. All gap entries have non-empty phase1_guidance
      4. weight_map entries exist for every component_bridge where megatron is not null AND the component has weight_structure entries (partial weight_map is acceptable for components with novel sub-modules)
      5. No empty required string fields in bridge_mapping (model, hf_source, megatron_family)
      6. gap_decisions.md exists and has one section per gap entry in bridge_mapping.gaps (per D-02)
    - If checks fail: return to Step 5.5 to fill gaps (e.g., add missing component_bridge entries). Max 3 rounds.
    - After 3 rounds still incomplete: write `phases/phase0/escalation.md` with list of incomplete items, exit `human_needed`

    **8. Step 9 — Validation** — Update checks:
    - Replace `model_spec_exists` with `hf_analysis_exists: true`
    - Add `reference_impl_analysis_exists: true`
    - Add `bridge_mapping_exists: true`
    - Add `bridge_mapping_component_bridge_non_empty: true`
    - Add `bridge_mapping_gaps_have_guidance: true` (all gap entries have phase1_guidance)
    - Keep `components_non_empty` (checks hf_analysis.components)
    - Keep `weight_structure_non_empty`
    - Keep `slice_hf_ckpt_path_resolved`
    - Remove `reference_contract_extracted` (absorbed into bridge_mapping)

    **9. Output Contract** — Update to reference three-document structure:
    ```
    run_dir/phases/phase0/
    ├── hf_analysis.yaml           (was model_spec.yaml — per D-04)
    ├── reference_impl_analysis.yaml  (NEW)
    ├── bridge_mapping.yaml        (NEW, absorbs reference_contract.yml — per D-05)
    ├── gap_decisions.md           (human-readable gap record — per D-02)
    ├── slice_report.json          (retained — per D-03)
    ├── attempts.jsonl             (optional, quality loop records)
    └── sliced_hf/                 (optional, only when slicing performed)
    ```

    **10. Loop Engineering Hooks** — Keep the existing pre-edit/post-edit hooks but add a note: "Phase 0 does NOT use the Loop FSM (per D-15). These hooks are retained for future extension but are inactive by default for Phase 0."

    **11. Create skills/adapt/references/phases/phase0/phase0_output_schema.yaml** — New file defining the updated phase0_output.yml structure:
    - Same top-level structure (phase, status, step_gate, steps, source, model, slice, checks)
    - Updated artifacts section: `hf_analysis_path`, `reference_impl_analysis_path`, `bridge_mapping_path`, `gap_decisions_path`, `slice_report_path`
    - Updated model section: add `megatron_family` field
    - Updated checks section: replace model_spec_exists with hf_analysis_exists, add bridge_mapping_exists and reference_impl_analysis_exists and bridge_mapping_component_bridge_non_empty and bridge_mapping_gaps_have_guidance
    - Keep backward compatibility note: "Legacy model_spec.yaml references are deprecated. Downstream phases should read bridge_mapping.yaml as the primary Phase 0 artifact."
  </action>

  <verify>
    <automated>grep -c "analyzing_megatron" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/phases/phase0/agent.md && grep -c "bridge_mapping" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/phases/phase0/agent.md && grep -c "quality_loop\|quality inner loop" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/phases/phase0/agent.md && grep -c "hf_analysis" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/phases/phase0/agent.md && grep "hf_analysis_exists" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/phases/phase0/phase0_output_schema.yaml && echo "PASS: all key sections present"</automated>
  </verify>

  <done>
    - skills/adapt/references/phases/phase0/agent.md contains states: analyzing_megatron, bridge_mapping, quality_loop (the 3 new states)
    - agent.md Step 5 invokes megatron-reference-analyzer
    - agent.md Step 5.5 defines the deterministic bridge step algorithm
    - agent.md Step 6 defines the quality inner loop with max 3 rounds
    - agent.md output contract references hf_analysis.yaml, reference_impl_analysis.yaml, bridge_mapping.yaml, gap_decisions.md
    - agent.md states Phase 0 does NOT use Loop FSM (per D-15)
    - phase0_output_schema.yaml exists with hf_analysis_path, reference_impl_analysis_path, bridge_mapping_path fields
    - phase0_output_schema.yaml checks section includes hf_analysis_exists, bridge_mapping_exists, bridge_mapping_component_bridge_non_empty
    - Every locked decision (D-01 through D-19) is reflected in the agent.md content
  </done>
</task>

</tasks>

<verification>
1. Pydantic models import successfully: `python3 -c "from skills.adapt.lib.schema import HfAnalysis, ReferenceImplAnalysis, BridgeMapping"`
2. YAML schema templates exist and reference the correct field names: `ls skills/adapt/knowledge_base/schema/{hf_analysis_schema,reference_impl_analysis_schema,bridge_mapping_schema}.yaml`
3. Phase 0 agent.md references all three new output files and all three new states
4. No deferred items (auto-create KB, unified validator, Megatron module design) appear in the agent.md
</verification>

<success_criteria>
- HfAnalysis Pydantic model preserves all current model_spec_llm.yaml fields
- BridgeMapping model matches D-16 schema structure exactly (component_bridge with hf/megatron/strategy/confidence/weight_map/behavioral_diff/delta, gaps with id/component/hf/megatron/decision/impact/phase1_guidance)
- Phase 0 agent.md defines 9-step state machine with quality inner loop
- All three YAML schema template files exist with complete field documentation
</success_criteria>

<output>
After completion, create `.planning/phases/06-phase0-redesign/06-01-SUMMARY.md`
</output>
