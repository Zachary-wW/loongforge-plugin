---
phase: 06-phase0-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md
  - skills/adapt/knowledge_base/examples/hf_analysis_llm.yaml
  - skills/adapt/knowledge_base/examples/reference_impl_analysis_llm.yaml
  - skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml
  - skills/adapt/knowledge_base/sources/source_digest_schema.md
autonomous: true
requirements: [P0R-02, P0R-07, P0R-08]

must_haves:
  truths:
    - "megatron-reference-analyzer SKILL.md defines a 4-stage process for extracting Megatron module information"
    - "hf_analysis_llm.yaml example preserves all fields from model_spec_llm.yaml under the new schema"
    - "bridge_mapping_llm.yaml example has at least 3 component_bridge entries and 2 gap entries"
    - "reference_impl_analysis_llm.yaml example shows Megatron module analysis for at least 2 modules"
  artifacts:
    - path: "skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md"
      provides: "Megatron reference analysis skill definition"
      contains: "Stage 1"
      min_lines: 200
    - path: "skills/adapt/knowledge_base/examples/hf_analysis_llm.yaml"
      provides: "Example hf_analysis for DeepSeek-V3"
      contains: "model_category: llm"
    - path: "skills/adapt/knowledge_base/examples/reference_impl_analysis_llm.yaml"
      provides: "Example reference_impl_analysis for DeepSeek-V3"
      contains: "megatron_family"
    - path: "skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml"
      provides: "Example bridge_mapping for DeepSeek-V3/DS-V4"
      contains: "component_bridge"
  key_links:
    - from: "skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md"
      to: "skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml"
      via: "output format matches schema template"
      pattern: "reference_impl_analysis"
    - from: "skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml"
      to: "skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml"
      via: "example follows schema structure"
      pattern: "component_bridge"
---

<objective>
Create the Megatron reference analyzer skill definition and concrete example YAML files that validate the three-document schema design against the DS V4 ground truth.

Purpose: The megatron-reference-analyzer is the new sub-skill that reads Megatron source code to produce reference_impl_analysis.yaml (per D-18). The example files validate that the schemas from Plan 01 can represent real model data — particularly the DS V4 case study which has 15 Megatron files, 33 LoongForge files, and 100+ weight mappings.

Output: New megatron-reference-analyzer SKILL.md, 3 example YAML files, updated source_digest_schema.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-phase0-redesign/06-CONTEXT.md

<!-- DS V4 Ground Truth reference paths for example validation -->
<!-- /Users/weizhihao/workspace/tmp_repo/0623/ground_truth/baidu/hac-aiacc/AIAK-Megatron/ -->
<!-- /Users/weizhihao/workspace/tmp_repo/0623/ground_truth/baidu/hac-aiacc/AIAK-Training-Omni/ -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write megatron-reference-analyzer SKILL.md</name>
  <files>skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md</files>

  <read_first>
    - skills/adapt/references/tools/hf-model-analyzer/SKILL.md (pattern to follow: stages, component identification rules, output schema, human_needed triggers)
    - skills/adapt/knowledge_base/schema/MEGATRON_COMPONENT_MAP.md (Megatron architecture: spec-driven assembly, TransformerLayerSubmodules slots, component lookup paths)
    - .planning/phases/06-phase0-redesign/06-CONTEXT.md (D-06: existing module depth only; D-08: no implementation design; D-18: new sub-skill)
  </read_first>

  <action>
    Create `skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md` following the same structure as hf-model-analyzer/SKILL.md (frontmatter, stages, component identification table, output schema, human_needed triggers). The skill is read-only — it never writes code, never opens PRs/issues (per D-06: "Phase 0 only analyzes existing Megatron modules").

    **Frontmatter:**
    ```yaml
    ---
    name: megatron-reference-analyzer
    description: LoongForge Phase 0 Megatron-side analysis skill. Reads Megatron source code, identifies existing module class signatures, init members, forward flow, and config fields. Produces reference_impl_analysis.yaml. Use when executing Phase 0 Step 5 of model adaptation.
    compatibility: Designed for LoongForge /loongforge:adapt workflow (Claude Code or similar)
    ---
    ```

    **Input:**
    ```
    megatron_path       -- Megatron-LM repo root (from run_inputs.yml repos.megatron or paths.megatron_path)
    component_list      -- List of HF component keys to look up (from hf_analysis.components keys)
    candidate_family    -- Candidate family name (from hf_analysis.candidate_family, used for KB lookup)
    kb_source_path       -- Optional KB source YAML for the candidate family (knowledge_base/sources/<type>/<family>.yaml)
    run_dir              -- Output artifact directory
    ```

    **Stage 1 — Module Discovery**
    For each component in component_list:
    1. Look up the Megatron module path using `MEGATRON_COMPONENT_MAP.md` Section 5 (component→file mapping). For standard components (attention, ffn, norm, etc.), the map provides the primary source file.
    2. If component is not in the map, use greedy search: `grep -r "class <ClassName>" <megatron_path>/megatron/core/ --include="*.py" -l`
    3. If no Megatron module is found for a component, record `found: false` and skip to next component (the bridge step will create a gap entry)
    4. Read the discovered source file, locate the target class, and record `class_name`, `source_file`, `base_classes`

    **Stage 2 — Signature Extraction**
    For each discovered module:
    1. Parse `__init__` signature: extract all `self.X = Y(...)` submodule assignments as `init_signature.params`. Each param entry: name (the attribute name, e.g., "linear_q_proj"), type_hint (the class used, e.g., "TEColumnParallelLinear"), default_value (None if conditional), description (brief role)
    2. Parse `forward` signature: extract method parameters as `forward_signature.inputs` (e.g., "hidden_states", "attention_mask", "context"). Extract return values as `forward_signature.outputs`
    3. Read `TransformerConfig` or `MLATransformerConfig` or model-specific config class referenced in `__init__`, record which `config.*` fields control this module's behavior as `config_fields_used`
    4. Identify `SubmoduleSpec` or `ModuleSpec` usage in `__init__` — record as `submodule_slots`. Each slot: slot_name (e.g., "core_attention"), slot_type (ModuleSpec or class), default_class, is_replaceable (True if the slot can be overridden in layer_spec)
    5. Scan `__init__` for `nn.Parameter`, `nn.Linear`, `register_buffer` — record as `weight_params`. Each: param_name (e.g., "e_score_correction_bias"), shape_hint (e.g., "[n_routed_experts]"), dtype

    **Stage 3 — Config Class Analysis**
    For the candidate family's config class (identified from KB source or by searching for `<family>Config` in the codebase):
    1. Read the config class source file
    2. Extract all field definitions as `ConfigClassAnalysis.fields`. Each: field_name, type_hint, default_value, description
    3. Record parent classes (e.g., `MLATransformerConfig`, `TransformerConfig`)
    4. Note which fields are new vs inherited from parent

    **Stage 4 — Write reference_impl_analysis.yaml**
    Write output following the schema from `knowledge_base/schema/reference_impl_analysis_schema.yaml`. Include all discovered modules, config classes, and cross-references.

    **Component identification table** (mirror hf-model-analyzer pattern but for Megatron side):

    | Component Key | Megatron Lookup Path |
    |---------------|---------------------|
    | `embedding` | `megatron/core/transformer/embedding.py` — `Embedding` class |
    | `positional_encoding` | `megatron/core/transformer/rotary_pos_embedding.py` — `RotaryEmbedding` or model-specific |
    | `attention` | `megatron/core/transformer/attention.py` — `SelfAttention` or `multi_latent_attention.py` — `MLASelfAttention` or `experimental_attention_variant/` |
    | `ffn` | `megatron/core/transformer/mlp.py` — `MLP` class |
    | `moe_gate` | `megatron/core/transformer/moe/router.py` — `Router` class |
    | `moe_experts` | `megatron/core/transformer/moe/experts.py` — `MLP` subclass |
    | `moe_shared_experts` | `megatron/core/transformer/moe/shared_experts.py` — `SharedExpertMLP` |
    | `norm` | `megatron/core/transformer/torch_norm.py` — `RMSNorm` or `LayerNorm` |
    | `decoder_layer` | `megatron/core/transformer/transformer_layer.py` — `TransformerLayer` |
    | `mtp` | `megatron/core/transformer/multi_token_prediction.py` — `GPTMultiTokenPrediction` |
    | `hyper_connection` | `megatron/core/transformer/hyper_connection.py` — `HyperConnectionModule` |

    For components not in this table (e.g., novel modules from hf_analysis.novel_modules): mark `found: false` with the component name — the bridge step will create gap entries.

    **human_needed triggers:**
    - megatron_path is not provided or does not contain `megatron/core/transformer/` directory
    - No modules can be discovered for ANY standard component (total failure)
    - KB source YAML for candidate_family references files that don't exist in the provided megatron_path

    **Key constraint (per D-06, D-08):** This skill ONLY reads and extracts. It does NOT design new Megatron modules, does NOT write code, and does NOT make implementation decisions. If a Megatron module doesn't exist for a component, it records `found: false` — that's all. Designing new modules is Phase 1's job.
  </action>

  <verify>
    <automated>grep -c "Stage 1\|Stage 2\|Stage 3\|Stage 4" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md && grep -c "reference_impl_analysis" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md && grep "found: false" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md && echo "PASS: skill defined with 4 stages"</automated>
  </verify>

  <done>
    - skills/adapt/references/tools/megatron-reference-analyzer/SKILL.md exists with 4 stages
    - Stage 1 discovers Megatron modules using MEGATRON_COMPONENT_MAP + greedy grep fallback
    - Stage 2 extracts init_signature, forward_signature, config_fields_used, submodule_slots, weight_params
    - Stage 3 analyzes config classes (field names, defaults, inheritance)
    - Stage 4 writes reference_impl_analysis.yaml following the schema template
    - Component identification table maps all standard components to Megatron lookup paths
    - human_needed triggers defined for missing/unreadable megatron_path
    - Skill explicitly states it does NOT design or implement new Megatron modules (per D-06, D-08)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create example YAML files and update source digest schema</name>
  <files>skills/adapt/knowledge_base/examples/hf_analysis_llm.yaml, skills/adapt/knowledge_base/examples/reference_impl_analysis_llm.yaml, skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml, skills/adapt/knowledge_base/sources/source_digest_schema.md</files>

  <read_first>
    - skills/adapt/knowledge_base/examples/model_spec_llm.yaml (current LLM example — all fields must be preserved in hf_analysis_llm.yaml)
    - skills/adapt/knowledge_base/schema/bridge_mapping_schema.yaml (from Plan 01 — bridge_mapping_llm.yaml must match this schema)
    - skills/adapt/knowledge_base/schema/reference_impl_analysis_schema.yaml (from Plan 01 — reference_impl_analysis_llm.yaml must match this schema)
    - skills/adapt/knowledge_base/sources/source_digest_schema.md (current schema — must add bridge_mapping references)
    - /Users/weizhihao/workspace/tmp_repo/0623/ground_truth/baidu/hac-aiacc/AIAK-Megatron/megatron/core/transformer/hyper_connection.py (GT: mHC module for reference_impl_analysis example)
    - /Users/weizhihao/workspace/tmp_repo/0623/ground_truth/baidu/hac-aiacc/AIAK-Megatron/megatron/core/transformer/moe/router.py (GT: router with hash+expert bias extensions)
    - /Users/weizhihao/workspace/tmp_repo/0623/ground_truth/baidu/hac-aiacc/AIAK-Training-Omni/configs/models/deepseek4/ckpt_convert/deepseek_v4_convert.yaml (GT: 100+ weight mappings for bridge_mapping example)
  </read_first>

  <action>
    **1. Create skills/adapt/knowledge_base/examples/hf_analysis_llm.yaml**
    This is the renamed/restructured version of model_spec_llm.yaml. Take the existing model_spec_llm.yaml content and:
    - Keep all fields identical (model_category, candidate_family, components, novel_modules, traps, special_features, weight_structure)
    - Add the new fields: `low_confidence_candidate: false` (DeepSeek-V3 has a KB entry), `fp32_modules` (empty for V3 — no strict FP32 modules known), `behavior_modifications` (empty for V3 baseline; DS V4 will have entries), `generation_constraints: []`, `config_precision_delta: []`
    - Header comment: "# hf_analysis.yaml complete example -- LLM (DeepSeek-V3). Supersedes model_spec.yaml."
    - This file serves as the LLM example; the existing model_spec_llm.yaml is NOT deleted yet (backward compat during transition)

    **2. Create skills/adapt/knowledge_base/examples/reference_impl_analysis_llm.yaml**
    Produce a realistic Megatron-side analysis example for DeepSeek-V3's Megatron reference. Based on the GT Megatron code and MEGATRON_COMPONENT_MAP.md, populate entries for these modules:

    - `mla_self_attention`: class_name=MLASelfAttention, source_file=megatron/core/transformer/multi_latent_attention.py, init_signature with linear_q_proj, linear_q_down_proj, linear_kv_proj, linear_q_up_proj (grouped output), core_attention, q_layernorm, kv_layernorm. config_fields_used: multi_latent_attention, q_lora_rank, kv_lora_rank, qk_nope_head_dim, qk_rope_head_dim, v_head_dim. submodule_slots: core_attention (replaceable), linear_q_proj (replaceable), linear_kv_proj (replaceable). weight_params: list the Q/KV projection parameters

    - `moe_router`: class_name=Router (or TopKRouter), source_file=megatron/core/transformer/moe/router.py, init_signature with config, num_experts, etc. config_fields_used: moe_router_score_function, num_experts. submodule_slots: none (self-contained). weight_params: weight (the gating weight parameter)

    - `hyper_connection_module`: class_name=HyperConnectionModule, source_file=megatron/core/transformer/hyper_connection.py, init_signature with mapping_proj, bias, alpha_pre, alpha_post, alpha_res. config_fields_used: enable_hyper_connections, num_residual_streams. submodule_slots: none. weight_params: mapping_proj.weight, mapping_proj.bias, alpha_pre, alpha_post, alpha_res, bias

    Also include a `mlatransformer_config` config class analysis with fields: hidden_size, num_attention_heads, multi_latent_attention, q_lora_rank, kv_lora_rank, etc.

    Header comment: "# reference_impl_analysis.yaml example -- LLM (DeepSeek-V3 Megatron reference)."

    **3. Create skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml**
    This is the most critical example — it demonstrates the full bridge_mapping structure with DS V4 as the reference case (the case study that motivated this redesign). Include:

    - Top-level: model=deepseek_v4, hf_source=modeling_deepseek_v4.py, megatron_family=deepseek_v3
    - component_bridge entries (at least 8):
      - `embedding`: hf=inline, megatron=[Embedding], strategy=reuse_ref, confidence=high, weight_map with word_embeddings→embedding mapping
      - `positional_encoding`: hf=DeepseekV4YarnRotaryEmbedding, megatron=[RotaryEmbedding], strategy=adapt_ref, confidence=medium, behavioral_diff for yarn vs standard rope
      - `attention`: hf=DeepseekV4Attention, megatron=[MLASelfAttention], strategy=adapt_ref, confidence=medium, weight_map entries for q_down/kv_down/wo_a/wo_b/q_norm/kv_norm (derived from GT deepseek_v4_convert.yaml name_map), behavioral_diff for CSA/HCA indexer absence
      - `ffn`: hf=DeepseekV4MLP, megatron=[MLP], strategy=reuse_ref, confidence=high, weight_map for gate_proj/up_proj/down_proj
      - `moe_gate`: hf=DeepseekV4MoEGate, megatron=[Router], strategy=adapt_ref, confidence=medium, weight_map for gate.weight and e_score_correction_bias, behavioral_diff for hash routing + expert bias
      - `moe_shared_experts`: hf=DeepseekV4MLP(shared), megatron=[SharedExpertMLP], strategy=adapt_ref, confidence=high
      - `hyper_connection`: hf=DeepseekV4HyperConnection, megatron=[HyperConnectionModule], strategy=adapt_ref, confidence=medium, weight_map for hc_attn_*/hc_ffn_* (derived from GT convert yaml)
      - `norm`: hf=DeepseekV4RMSNorm, megatron=[RMSNorm], strategy=reuse_ref, confidence=high
    - gaps entries (at least 3):
      - G1: component=csa_indexer, hf=DeepseekV4CSAIndexer, megatron=NEW, impact=critical, decision="New module needed in Megatron experimental_attention_variant/", phase1_guidance="Implement CSA/HCA indexer as a new module under experimental_attention_variant/ based on HF DSAIndexer pattern and Megatron issue #4468 clues"
      - G2: component=hash_router, hf=DeepseekV4HashRouter, megatron=NEW, impact=high, decision="New routing logic needed in Megatron Router", phase1_guidance="Extend Megaton Router with hash-based routing; see Megatron-LM issue #4468 for community implementation reference"
      - G3: component=clamped_swiglu, hf=DeepseekV4MLP (behavior_modification), megatron=MLP (no clamp), impact=medium, decision="Behavior modification in Omni model-specific code", phase1_guidance="Add activation clamp in model-specific MLP; do not modify shared Megatron MLP"
    - validator_requirements: ["Phase 1 must verify CSA/HCA indexer forward output shape matches HF", "Phase 2 must map all 100+ weight keys from deepseek_v4_convert.yaml", "Phase 3 must verify loss diff < 1e-3 for both dense and MoE layers"]
    - implementation_contract: required_integration_level=framework_extension, allow_shared_framework_changes=true, required_components for CSA/HCA indexer
    - Header comment: "# bridge_mapping.yaml example -- DS V4 (motivating case study). Demonstrates component_bridge with weight_map, gaps for novel components, and validator_requirements."

    **4. Update skills/adapt/knowledge_base/sources/source_digest_schema.md**
    Read the current file. Add documentation for the new `bridge_mapping_path` and `reference_impl_analysis_path` fields that Phase 0 now produces. Add a section: "Phase 0 Output Changes (v2)" documenting that:
    - model_spec_path is deprecated in favor of hf_analysis_path
    - New fields: reference_impl_analysis_path, bridge_mapping_path, gap_decisions_path
    - Phase 1 should read bridge_mapping.yaml as the primary input, not model_spec.yaml
    - reference_contract_path is deprecated (absorbed into bridge_mapping.yaml)
    - Legacy fields remain readable for backward compat
  </action>

  <verify>
    <automated>grep "model_category: llm" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/knowledge_base/examples/hf_analysis_llm.yaml && grep "megatron_family" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/knowledge_base/examples/reference_impl_analysis_llm.yaml && grep "component_bridge" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml && grep -c "^  - hf:" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/knowledge_base/examples/bridge_mapping_llm.yaml && grep "bridge_mapping_path" /Users/weizhihao/workspace/agent_skills/loongforge-plugin/skills/adapt/knowledge_base/sources/source_digest_schema.md && echo "PASS: all example files and schema update present"</automated>
  </verify>

  <done>
    - hf_analysis_llm.yaml exists with all fields from model_spec_llm.yaml plus new fields (low_confidence_candidate, fp32_modules, behavior_modifications)
    - reference_impl_analysis_llm.yaml has at least 3 module entries (mla_self_attention, moe_router, hyper_connection_module) and a config class analysis
    - bridge_mapping_llm.yaml has at least 8 component_bridge entries and 3 gap entries with weight_map and phase1_guidance
    - bridge_mapping_llm.yaml weight_map entries reference actual parameter names from GT deepseek_v4_convert.yaml (q_down, kv_down, wo_a, wo_b, etc.)
    - source_digest_schema.md documents bridge_mapping_path and reference_impl_analysis_path as new Phase 0 output fields
    - No deferred items (auto-create KB, unified validator, Megatron module design) appear in any example
  </done>
</task>

</tasks>

<verification>
1. megatron-reference-analyzer SKILL.md exists with 4 stages
2. All 3 example YAML files exist and validate against their schema templates (field names match)
3. bridge_mapping_llm.yaml contains realistic weight_map entries derived from GT convert YAML
4. source_digest_schema.md documents the new Phase 0 output fields
</verification>

<success_criteria>
- megatron-reference-analyzer defines a clear, stage-by-stage process for reading Megatron source and extracting module signatures (per D-18)
- Example files prove the three-document schema can represent real DS V4 data
- bridge_mapping_llm.yaml gap entries demonstrate the gap detection pattern (per D-07) with phase1_guidance
- No example implements a deferred idea
</success_criteria>

<output>
After completion, create `.planning/phases/06-phase0-redesign/06-02-SUMMARY.md`
</output>
