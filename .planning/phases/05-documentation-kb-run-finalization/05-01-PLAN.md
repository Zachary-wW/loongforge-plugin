---
phase: 05-documentation-kb-run-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adapt/SKILL.md
  - skills/adapt/references/loop_engineering/README.md
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "SKILL.md describes the 12-state FSM (PROBE through EXIT), the four repos: user inputs, the maker-checker split (Edit agent != Diagnose agent), the three-axis termination budget (max_attempts_per_phase=5, max_attempts_per_run=25, max_wallclock_minutes=240), and a When NOT to Use guard listing trivial-fix and no-validator cases"
    - "SKILL.md preserves all existing mechanics sections verbatim (Reading Order, Phase Dispatch Rules, Checkpoint Protocol, Bulk Log Externalization, Autonomous Mode)"
    - "SKILL.md includes the three-layer loop framing (Inner/Middle/Outer) per D-05"
    - "loop_engineering/README.md cites se.rpcx.io/04, /08, /12 as source articles and maps all 21 principles (P1-P21) to concrete implementation files/functions in the codebase"
    - "loop_engineering/README.md includes the three-layer loop framing as its organizing principle"
  artifacts:
    - path: "skills/adapt/SKILL.md"
      provides: "Loop-first architecture framing + preserved mechanics"
      contains: "When NOT to Use"
    - path: "skills/adapt/references/loop_engineering/README.md"
      provides: "P1-P21 principle-to-implementation mapping"
      contains: "P1"
  key_links:
    - from: "skills/adapt/SKILL.md"
      to: "skills/adapt/lib/loop_controller.py"
      via: "FSMState and ExitReason enum references"
      pattern: "FSMState|ExitReason"
    - from: "skills/adapt/references/loop_engineering/README.md"
      to: "skills/adapt/lib/loop_controller.py"
      via: "P1, P2, P3, P7, P8, P18 principle mappings"
      pattern: "LoopState\\.from_disk|check_budget|ExitReason"
    - from: "skills/adapt/references/loop_engineering/README.md"
      to: "skills/adapt/lib/diagnose_classifier.py"
      via: "P16 maker-checker mapping"
      pattern: "classify_failure|DiagnoseClassification"
---

<objective>
Surgically rewrite SKILL.md to surface loop-first architecture (DOC-01) and create loop_engineering/README.md with P1-P21 principle-to-implementation mapping (DOC-02).

Purpose: Users and maintainers must understand the closed-loop architecture before starting a run, and understand WHY each design choice was made via the principle traceability map.

Output: Rewritten skills/adapt/SKILL.md and new skills/adapt/references/loop_engineering/README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-kb-run-finalization/05-CONTEXT.md
@.planning/phases/05-documentation-kb-run-finalization/05-RESEARCH.md
@.planning/research/STACK.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From skills/adapt/lib/loop_controller.py:
```python
class FSMState(str, Enum):
    PROBE = "probe"
    EDIT = "edit"
    PR = "pr"
    MERGE_BASE = "merge_base"
    VALIDATE = "validate"
    DIAGNOSE = "diagnose"
    ISSUE = "issue"
    FIX_PR = "fix_pr"
    REVIEW = "review"
    MERGE_FIX = "merge_fix"
    RERUN = "rerun"
    EXIT = "exit"

class ExitReason(str, Enum):
    VALIDATOR_PASSED = "validator_passed"
    VALIDATOR_PASSED_AFTER_FIX = "validator_passed_after_fix"
    EXHAUSTED = "exhausted"
    ESCALATED = "escalated"
    BASE_ONLY = "base_only"
    HUMAN_NEEDED = "human_needed"
```

From skills/adapt/lib/schema.py:
```python
class LoopBudget(BaseModel):
    max_attempts_per_phase: int = Field(5, ge=1, le=50)
    max_attempts_per_run: int = Field(25, ge=1, le=500)
    max_wallclock_minutes: int = Field(240, ge=10, le=10_080)
    escalation: Literal["human_needed", "autonomous_blocked"] = "human_needed"

class ReposBlock(BaseModel):
    hf_impl: HFImplSpec
    hf_ckpt: HFCkptSpec
    loongforge: RepoSpec
    megatron: RepoSpec
```

From skills/adapt/lib/diagnose_classifier.py:
```python
class DiagnoseClassification(str, Enum):
    CODE_BUG = "code-bug"
    FLAKY = "flaky"
    WRONG_DIRECTION = "wrong-direction"
    NEEDS_HUMAN = "needs-human"
```

From skills/adapt/lib/loop_controller.py:
```python
def run_phase_loop(run_dir, phase, gh, budget, dry_run=False, max_iterations=100, repos_info=None) -> ExitReason
def check_budget(budget, attempt, total_attempts_used, run_start_time) -> ExitReason | None
```

From skills/adapt/SKILL.md (PRESERVED SECTIONS -- do NOT modify):
- "## Reading Order" (lines 27-36)
- "## Claude Code Harness Reuse" (lines 38-43)
- "## Input Schema Markers" (lines 45-48)
- "## State Source of Truth" (lines 50-67)
- "## Startup Runner" (lines 69-76)
- "## Six Phases" (lines 78-89)
- "## Phase Dispatch Rules" (lines 91-104)
- "## Phase-internal Step Enforcement" (lines 106-112)
- "## Validation Hook Concept" (lines 114-133)
- "## /loop Boundary" (lines 135-142)
- "## Bulk Log Externalization (SAFE-03)" (lines 144-146)
- "## Checkpoint Protocol" (lines 148-160)
- "## Autonomous Mode" (lines 162-164)

Three-layer loop framing (D-05 canonical source):
| Layer | Scope | Coordination Bus |
|-------|-------|-------------------|
| Inner | Phase-internal self-repair (attempts.jsonl) | Disk files |
| Middle | GitHub PR/issue cycle (loop controller) | GitHub (gh CLI) |
| Outer | Multi-model replay (future) | Run directory |

Key insight: "The plugin itself is what the loop fixes." The loop doesn't adapt the model; it adapts the plugin's own bugs out.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgically rewrite SKILL.md with loop-first architecture framing (DOC-01, per D-01, D-05)</name>
  <files>skills/adapt/SKILL.md</files>
  <read_first>
    skills/adapt/SKILL.md
    skills/adapt/lib/loop_controller.py
    skills/adapt/lib/schema.py
    skills/adapt/lib/diagnose_classifier.py
    skills/adapt/loop_templates/phaseN/repair.md
  </read_first>
  <behavior>
    - SKILL.md frontmatter preserved (name, description fields)
    - New top-level sections added BEFORE preserved mechanics sections
    - ALL existing mechanics sections preserved verbatim (Reading Order, Claude Code Harness Reuse, Input Schema Markers, State Source of Truth, Startup Runner, Six Phases, Phase Dispatch Rules, Phase-internal Step Enforcement, Validation Hook Concept, /loop Boundary, Bulk Log Externalization, Checkpoint Protocol, Autonomous Mode)
    - No mechanics section text is modified
  </behavior>
  <action>
    Rewrite skills/adapt/SKILL.md with the following surgical changes, per D-01 (preserve mechanics, replace architecture framing) and D-05 (integrate three-layer loop framing):

    1. PRESERVE the frontmatter exactly as-is (lines 1-8: name, description).

    2. REPLACE the "# /loongforge:adapt — LoongForge Model Adaptation" heading and "This is the plugin entrypoint..." intro block (lines 9-25) with a loop-first architecture framing that includes:

    a. Title: "# /loongforge:adapt — LoongForge Model Adaptation"
    b. "## Loop-First Architecture" section containing:
       - One-paragraph summary: When `repos:` is present in `run_inputs.yml`, the skill operates as a loop-engineering system: every code change goes through a closed loop on external GitHub repos until all phase validators pass.
       - Three Nested Loops table (D-05): Inner (phase-internal self-repair, disk files), Middle (GitHub PR/issue cycle, gh CLI), Outer (multi-model replay, run directory -- future). Note the key insight: "The plugin itself is what the loop fixes."
       - 12-State FSM diagram: PROBE -> EDIT -> PR -> MERGE_BASE -> VALIDATE -> (DIAGNOSE -> ISSUE -> FIX_PR -> REVIEW -> MERGE_FIX -> RERUN)* -> EXIT. List all 12 FSMState values and all 6 ExitReason values by name (reference loop_controller.py FSMState and ExitReason enums).
       - Maker-Checker Split paragraph: Edit/PR-author agent and Diagnose agent are distinct sub-agents. Diagnose is read-only and classifies failures as code-bug | flaky | wrong-direction | needs-human (reference diagnose_classifier.py DiagnoseClassification). wrong-direction short-circuits to human_needed.
       - Three-Axis Budget paragraph: max_attempts_per_phase=5, max_attempts_per_run=25, max_wallclock_minutes=240. Any axis tripping forces exit reason exhausted or human_needed, never passed (reference schema.py LoopBudget).
       - GitHub as Coordination Bus paragraph: PRs, issues, and merges coordinate the loop across processes and sessions; this is NOT an in-session agent loop.

    c. "## When NOT to Use This Loop" section listing:
       - Trivial fixes (one-line config changes with known validators that always pass)
       - No validator exists for the target phase
       - Single-run, no-replay scenarios where manual commit-and-push suffices
       - Cases where the model adapter has full local write access and does not need PR-based coordination

    d. "## Loop Invocation" section:
       - Document the `repos:` gated behavior: when repos: is present in run_inputs.yml, loop engineering is active; when absent, legacy behavior runs unchanged (COMPAT-01).
       - Document the four URL inputs: --hf-impl-url, --hf-ckpt-url, --loongforge-repo, --megatron-repo (with associated --*-ref and --*-subpath variants).
       - Document --dry-run flag: FakeGhClient selected, no live gh calls, no GPU.
       - Document --resume flag: reconstructs FSM state from disk, reconciles remote PR/issue state.

    e. "## End-of-Run Housekeeping" section:
       - On run completion, all auxiliary bot-created issues should be closed with a summary comment linking the run digest.
       - Bot PRs/issues must consistently carry labels: loongforge-adapt, run-<id>, phase-<N>.
       - Run an end-of-run housekeeping pass: verify every bot-created PR and issue has the required labels; close any stranded auxiliary issues.

    3. PRESERVE all existing mechanics sections verbatim (Reading Order through Autonomous Mode), positioned AFTER the new loop-first framing sections. Do NOT modify a single word of these sections.

    4. Update the description in frontmatter to reflect loop-engineering capability: description should mention loop-engineering mode, repos: gated behavior, and the closed-loop adaptation cycle.

    CRITICAL: The diff between old and new SKILL.md must show ONLY: (a) new sections inserted at the top, (b) the intro paragraph replaced, (c) frontmatter description updated. No changes to any mechanics section text. If a mechanics section text is modified, the rewrite is incorrect per D-01.
  </action>
  <verify>
    <automated>cd /Users/weizhihao/workspace/agent_skills/loongforge-plugin && grep -c "When NOT to Use" skills/adapt/SKILL.md && grep -c "Three Nested Loops\|Three-Axis Budget\|Maker-Checker Split\|12-State FSM\|Loop-First Architecture" skills/adapt/SKILL.md && grep -c "## Reading Order" skills/adapt/SKILL.md && grep -c "## Phase Dispatch Rules" skills/adapt/SKILL.md && grep -c "## Checkpoint Protocol" skills/adapt/SKILL.md && grep -c "## Autonomous Mode" skills/adapt/SKILL.md && grep -c "## Bulk Log Externalization" skills/adapt/SKILL.md && grep -c "VALIDATOR_PASSED\|EXHAUSTED\|ESCALATED\|BASE_ONLY\|HUMAN_NEEDED" skills/adapt/SKILL.md && grep -c "max_attempts_per_phase\|max_attempts_per_run\|max_wallclock_minutes" skills/adapt/SKILL.md && grep -c "code-bug\|flaky\|wrong-direction\|needs-human" skills/adapt/SKILL.md</automated>
  </verify>
  <done>
    SKILL.md contains loop-first architecture sections (Loop-First Architecture, When NOT to Use, Loop Invocation, End-of-Run Housekeeping) with three-layer framing, 12-state FSM, maker-checker split, three-axis budget, and GitHub-as-bus description. All mechanics sections (Reading Order, Phase Dispatch Rules, Checkpoint Protocol, Bulk Log Externalization, Autonomous Mode, etc.) preserved verbatim. Frontmatter description updated.
  </done>
  <acceptance_criteria>
    - skills/adapt/SKILL.md contains "## Loop-First Architecture" heading
    - skills/adapt/SKILL.md contains "## When NOT to Use This Loop" heading
    - skills/adapt/SKILL.md contains "## Loop Invocation" heading
    - skills/adapt/SKILL.md contains "## End-of-Run Housekeeping" heading
    - skills/adapt/SKILL.md contains the string "max_attempts_per_phase=5"
    - skills/adapt/SKILL.md contains all 6 ExitReason value names: "validator_passed", "validator_passed_after_fix", "exhausted", "escalated", "base_only", "human_needed"
    - skills/adapt/SKILL.md contains all 4 DiagnoseClassification values: "code-bug", "flaky", "wrong-direction", "needs-human"
    - skills/adapt/SKILL.md contains "## Reading Order" section heading (preserved)
    - skills/adapt/SKILL.md contains "## Phase Dispatch Rules" section heading (preserved)
    - skills/adapt/SKILL.md contains "## Checkpoint Protocol" section heading (preserved)
    - skills/adapt/SKILL.md contains "## Bulk Log Externalization" section heading (preserved)
    - skills/adapt/SKILL.md contains "## Autonomous Mode" section heading (preserved)
    - skills/adapt/SKILL.md frontmatter contains "loop-engineering" in description field
    - No mechanics section content was modified from original (spot-check Reading Order first item still references "knowledge_base/schema/EXIT_CONTRACT.md")
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create loop_engineering/README.md with P1-P21 principle-to-implementation mapping (DOC-02, per D-05)</name>
  <files>skills/adapt/references/loop_engineering/README.md</files>
  <read_first>
    .planning/research/STACK.md
    skills/adapt/lib/loop_controller.py
    skills/adapt/lib/gh_client.py
    skills/adapt/lib/diagnose_classifier.py
    skills/adapt/lib/validator_wrapper.py
    skills/adapt/lib/schema.py
    skills/adapt/lib/resume.py
    skills/adapt/scripts/run.py
    skills/adapt/lib/jsonl.py
    skills/adapt/loop_templates/phaseN/repair.md
  </read_first>
  <behavior>
    - README.md cites se.rpcx.io/04, /08, /12 as source articles
    - All 21 principles (P1-P21) mapped to concrete implementation files/functions
    - Three-layer loop framing as organizing principle
    - Each principle entry has: principle number, quote/summary, source article, implementation file+function
  </behavior>
  <action>
    Create the directory skills/adapt/references/loop_engineering/ and the file README.md within it.

    The README.md structure:

    1. Title: "# Loop Engineering Reference — Adapt Skill"
    2. Intro paragraph: This document traces the loop-engineering design of the adapt skill to its foundational principles from three articles on se.rpcx.io (04, 08, 12). Each principle is mapped to the concrete implementation in this codebase.
    3. "## Three Nested Loops" section (D-05): The same three-layer table as in SKILL.md (Inner/Middle/Outer) as the organizing principle for the entire document. Brief paragraph explaining how each principle applies to one or more layers.

    4. "## Principles from se.rpcx.io/04 — Ralph Loop" section with P1-P7:
       For EACH principle, create a subsection with:
       - **P{N}**: "{principle name/quote}" — one-line summary of the principle
       - **Source:** se.rpcx.io/04 (Ralph Loop)
       - **Implementation:** {specific file and function/class that implements this, with a 1-2 sentence explanation}

       Use the exact P1-P7 content from STACK.md Part 1 table as the authoritative source. Map each to:
       - P1: skills/adapt/lib/loop_controller.py — LoopState.from_disk(run_dir, phase) reconstructs FSM state from loop_state.yml + attempts.jsonl tail every invocation; no in-memory conversation state persists across iterations
       - P2: skills/adapt/lib/loop_controller.py — check_budget() enforces all three budget axes; ExitReason returned when any axis breached
       - P3: skills/adapt/lib/validator_wrapper.py — run_validator() result is the only legitimate exit signal; loongforge-phase-gate reading phaseN_output.yml is the gate
       - P4: skills/adapt/loop_templates/phaseN/repair.md — escape_hatch paragraph in every repair prompt; skills/adapt/lib/diagnose_classifier.py write_escalation() writes escalation.md
       - P5: skills/adapt/lib/loop_controller.py — run_phase_loop() is a single-process re-entrant entrypoint; each iteration forks gh CLI via GhClient; skills/adapt/lib/gh_client.py RealGhClient._run() wraps subprocess.run
       - P6: skills/adapt/loop_templates/phaseN/repair.md — versioned Jinja2 template under loop_templates/; prompts are code, not inline strings
       - P7: skills/adapt/lib/diagnose_classifier.py — classify_failure() is mandatory before every Fix-PR; DiagnoseClassification.WRONG_DIRECTION short-circuits to human_needed

    5. "## Principles from se.rpcx.io/08 — Goal Workflow" section with P8-P13:
       - P8: skills/adapt/lib/loop_controller.py — _transition() writes typed YAML/JSON artifacts (loop_state.yml, attempts.jsonl) on every state change; next state reads them
       - P9: Phase-internal repair loop (run_phase_loop) is fully autonomous; cross-phase transition stays gated by user (Checkpoint Protocol in SKILL.md)
       - P10: skills/adapt/lib/validator_wrapper.py — ValidatorResult.status == "passed" is the only positive exit; ExitReason references specific validator name + status
       - P11: skills/adapt/lib/diagnose_classifier.py — DiagnoseResult.suggested_fix_summary goes into fix-PR body as advisory text, not into the diff; Edit agent decides what to change
       - P12: One GitHub issue = one validator failure = one fix-PR (ISSUE-02); skills/adapt/lib/gh_client.py open_issue() creates one issue per failure
       - P13: phases/phaseN/decision_log.md written after successful loop; linked from merged PR body

    6. "## Principles from se.rpcx.io/12 — Loop Engineering" section with P14-P21:
       - P14: This refactor produces a loop-controller spec (loop_controller.py run_phase_loop), not a smarter Phase-N prompt
       - P15: Five essentials mapped: Automations = --resume (run.py), Skills = adapt skill itself, Connectors = gh CLI (gh_client.py), Sub-agents = phase-N + diagnose (diagnose_classifier.py), State = attempts.jsonl + loop_state.yml
       - P16: skills/adapt/lib/diagnose_classifier.py — classify_failure() is read-only, never writes code or calls gh; Edit agent (loop_controller.py PR/FIX_PR states) does the writing
       - P17: Hill-climbing: goal (validator pass) + metric (ValidatorResult.status) + change (fix-PR diff) + measure (re-run validator after merge) = autonomous improvement; each phase loop enumerates all four
       - P18: skills/adapt/lib/loop_controller.py — check_budget() before every VALIDATE; budget exhaustion forces exit human_needed/autonomous_blocked, never passed
       - P19: Four conditions met: repeats per new model, validators automatable, GPU dominates cost, gh+Python+logs = senior-engineer toolchain; loop is justified
       - P20: Cross-phase checkpoint stays gated (SKILL.md Checkpoint Protocol); comprehension_summary.md (lib/summary_generator.py) addresses comprehension debt
       - P21: Single-laptop-open architecture; --resume (run.py resume_run_dir()) is enough; no daemon/supervisor

    7. "## Synthesized FSM" section: Include the ASCII FSM diagram from STACK.md Part 1 (the INIT -> PROBE -> EDIT -> ... diagram).

    8. "## Hard Do Not Use List" section: Summarize the key prohibitions from STACK.md Part 3: no webhooks/GitHub App, no GitHub Actions hosting, no asyncio, no SQLite, no retrying validator failures, no free-form Claude self-report as exit, no same sub-agent for Edit+Diagnose, no auto-merge without re-running validator.

    NOTE on source URLs: The se.rpcx.io/04, /08, /12 URLs returned 404 as of research date. Cite them anyway as the original source locations. The content is preserved in this repo's STACK.md and CLAUDE.md.

    CRITICAL: Every implementation reference must point to a real file and a real function/class that exists in the codebase. Reference stable public interfaces (LoopState.from_disk, run_phase_loop, classify_failure, check_budget, ExitReason, FSMState), not internal helpers (_transition, _advance_attempt, _reconstruct_validator_result). If any referenced name does not exist in the codebase, the README is incorrect.
  </action>
  <verify>
    <automated>cd /Users/weizhihao/workspace/agent_skills/loongforge-plugin && test -f skills/adapt/references/loop_engineering/README.md && grep -c "^### P[0-9]" skills/adapt/references/loop_engineering/README.md && grep -c "loop_controller.py\|diagnose_classifier.py\|validator_wrapper.py\|schema.py\|gh_client.py\|run.py\|resume.py\|jsonl.py\|repair.md" skills/adapt/references/loop_engineering/README.md && grep -c "se.rpcx.io/04\|se.rpcx.io/08\|se.rpcx.io/12" skills/adapt/references/loop_engineering/README.md</automated>
  </verify>
  <done>
    skills/adapt/references/loop_engineering/README.md exists with all 21 principles (P1-P21) mapped to concrete implementation files/functions, three-layer loop framing as organizing principle, source article citations, and FSM diagram. Every referenced file and function name exists in the codebase.
  </done>
  <acceptance_criteria>
    - skills/adapt/references/loop_engineering/README.md file exists
    - README.md contains "### P1" through "### P21" subsection headings (21 principle entries)
    - README.md contains "se.rpcx.io/04" (Ralph Loop source citation)
    - README.md contains "se.rpcx.io/08" (Goal Workflow source citation)
    - README.md contains "se.rpcx.io/12" (Loop Engineering source citation)
    - README.md contains "Three Nested Loops" section heading
    - README.md references "loop_controller.py" at least 5 times (P1, P2, P3, P8, P18, etc.)
    - README.md references "diagnose_classifier.py" (P7, P16)
    - README.md references "validator_wrapper.py" (P3, P10)
    - README.md references "gh_client.py" (P5, P15)
    - README.md contains "LoopState.from_disk" (P1 mapping to stable public interface)
    - README.md contains "run_phase_loop" (P5, P14, P17 mapping)
    - README.md contains "classify_failure" (P7, P16 mapping)
    - README.md contains "check_budget" (P2, P18 mapping)
    - README.md contains "Hard Do Not Use" section
    - README.md contains the FSM ASCII diagram (PROBE, VALIDATE, DIAGNOSE, EXIT, etc.)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. SKILL.md contains loop-first architecture framing with all required sections
2. SKILL.md preserves all existing mechanics sections unchanged
3. loop_engineering/README.md maps all P1-P21 to real implementation artifacts
4. All referenced file paths and function names exist in the codebase
5. Three-layer loop framing appears prominently in both documents
6. pytest skills/adapt/tests/ still green (no code changes, documentation only)
</verification>

<success_criteria>
- SKILL.md rewritten per D-01 (surgical: new framing, preserved mechanics) and D-05 (three-layer framing)
- loop_engineering/README.md created per DOC-02 with complete P1-P21 mapping
- Both documents cross-reference the same FSM architecture consistently
- No code files modified (documentation only)
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-kb-run-finalization/05-01-SUMMARY.md`
</output>
